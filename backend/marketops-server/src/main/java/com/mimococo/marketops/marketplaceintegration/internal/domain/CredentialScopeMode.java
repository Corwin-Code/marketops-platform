package com.mimococo.marketops.marketplaceintegration.internal.domain;

/** Declared coverage of a credential: the whole account, or exactly its active store-scope rows. */
public enum CredentialScopeMode {
    ACCOUNT,
    STORE_SET
}
