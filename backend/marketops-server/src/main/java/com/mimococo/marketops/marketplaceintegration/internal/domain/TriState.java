package com.mimococo.marketops.marketplaceintegration.internal.domain;

/** Explicit yes/no answer that keeps an unrecorded fact visible as UNKNOWN. */
public enum TriState {
    YES,
    NO,
    UNKNOWN
}
