package com.mimococo.marketops.marketplaceintegration.internal.domain;

/** Recorded availability of a capability for one concrete subject. */
public enum Availability {
    UNKNOWN,
    UNAVAILABLE,
    AVAILABLE
}
