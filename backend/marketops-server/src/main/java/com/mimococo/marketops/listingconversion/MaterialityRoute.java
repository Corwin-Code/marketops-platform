package com.mimococo.marketops.listingconversion;

/** Where the final approval of an action goes, decided on two independent axes. */
public enum MaterialityRoute {
    MATERIAL_IMPACT,
    ORDINARY_IMPACT,
    MATERIALITY_UNRESOLVED
}
