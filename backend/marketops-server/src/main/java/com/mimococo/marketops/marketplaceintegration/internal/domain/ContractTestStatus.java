package com.mimococo.marketops.marketplaceintegration.internal.domain;

/** Recorded standing of a capability's or endpoint's contract test. */
public enum ContractTestStatus {
    NOT_IMPLEMENTED,
    FAILING,
    PASSING
}
