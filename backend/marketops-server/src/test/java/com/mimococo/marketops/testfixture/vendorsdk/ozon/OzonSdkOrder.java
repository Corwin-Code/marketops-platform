package com.mimococo.marketops.testfixture.vendorsdk.ozon;

/** Local stand-in for a vendor SDK model; no real Marketplace SDK is a dependency. */
public record OzonSdkOrder(String vendorIdentifier) {
}
