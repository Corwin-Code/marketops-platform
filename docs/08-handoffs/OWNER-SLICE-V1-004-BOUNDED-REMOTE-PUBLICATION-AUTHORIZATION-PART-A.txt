我授权 Fable 5.1 对 `Corwin-Code/marketops-platform` 执行 SLICE-V1-004 的本地文档收尾，以及本次有界 Level 3 远程发布与 CI 核验。

准确起点：
- Head：`457935975655abdd2d77cc95e97c00ae45650ef5`
- Tree：`c90c5ca0ea44cbf5820bad8a4378cc44c98b8409`
- 命名分支：`claude/slice-v1-004-root-cause-rework-r1`

本次允许：
1. 在既有本地文档权限内处理 `DOC-SYNC-NOTE-01`，并保存本授权、必要的执行收据和交接证据。除这一文档备注和本次授权／交付存证，不新增其他修改。
2. 发布上述准确起点，或它仅包含上述有限文档变化的线性后继。执行者须记录实际候选 Head／Tree、父提交和逐文件差异；不得替换已经关闭的工程对象。
3. 只对该命名分支执行不改写历史的 push；创建或更新一份以实际 `main` 为 base 的 Draft PR；触发和读取现有、经核验不包含部署或真实 Provider 调用的隔离 CI，收集实际结果。
4. 一次授权覆盖上述普通读取、文档校验、本地文档 checkpoint、push、Draft PR 操作和适用 CI 核验，不逐命令要求 Owner 再次批准。

本次不授权 Ready、merge、auto-merge、直接写 main、force-push、rebase、改写已接受提交、删除分支、改变 Ruleset／保护规则／权限／Secrets、放宽 CI、修改产品代码或迁移、部署、生产迁移、独立 Level 2 环境、真实 Provider／账户调用、Gate EV／E、Pilot、生产写或下一 Slice 实施。

原工程关闭对象保持 `f71d4c8c2bdf5dc6497d7951d1cd5b12b0122f10` / `b8a9e7d0450d24f2b58b95d6b370daf9cd5e5e47`；27/27 Finding 关闭、54/12/3 验收分层、六项外部义务及全部前序发布义务均不变。

执行以如下 B 部分的准确范围为限；遇到超出范围的真实冲突，只报告最小差异，不自行扩大权限。
