# W6 applicability review — prepared only

The original five-file patch remains mechanically applicable to clean W6 `3ed3f4c87c336cb07188e470528f328358fb279f` / tree `4e73aa0e7c30fe470528ecd5287b55a9c55e5ff1`: all five inputs equal their original W2 hashes, all prepared artifacts match their manifest, Python AST parsing passes, and read-only `git apply --check` passes. `candidate-five-file-sync.patch` is an unchanged copy. No repository file or pending status was modified.

The old PLAN's W2 preparation identity and manifest's W3 preflight should remain historical provenance. Use the new `review.json` for W6 applicability; the old function probe is not a full future-state repository run. No adjustment to the five patch contents is currently needed.

Current `FINDING-CLOSURE-MATRIX.json` has exactly 22 frozen IDs and `S3-AC-REWORK-STATUS.json` has exactly 200 accepted criterion texts. Every row is still `REWORK_EVIDENCE_ASSEMBLED_VERIFICATION_PENDING`. All 24 release rows remain blocking without real evidence. The patch must stay inactive until Root has completed the full local/remote proof and each individual assessment listed in `review.json`.

The proposed phase is Codex engineering assessment complete, followed by independent Controller review. The suggested row statuses are descriptive engineering statements, not new normative authority. Do not emit a Controller verdict or Contract §19 final closure. AC200 includes actual full regression, governance/readiness, security and unchanged thresholds as well as the unresolved BLOCKER/MAJOR condition; record the executed components and retain independent closure pending. Do not reduce its accepted text to “Controller approval.”

Matrix metadata (`document_type`, `rework_head`, notes and evidence references) and the R1 README/final handoff need separate evidence-based updates beyond this five-file patch. Each finding needs complete frozen required-rework/verification coverage, positive/negative proof, same-class/transitive assessment and exact source/run identity. Each AC001–199 needs its own behavior, assertion/measurement, proof scope and rationale; mappings or a common green suite count are insufficient. Preserve historical failures and their actual original identities. Explain `closure_claim_made=false` as no independent final closure claim alongside a separate explicit Codex engineering assessment field.

Before applying, recheck hashes and patch applicability. After applying, run actual `make governance` with named results and source bindings; it covers both validators and full Python discovery. Two prepared new methods should add two to baseline 410 if discovery is otherwise unchanged, but report actual counts. Confirm non-noop authority mutations refuse false Controller/Owner/Ready/merge/production/Gate EV claims; preserve exact-byte tests. Check exact 22/200/24 sets, per-row evidence and release restrictions, then publish append-only to the same Draft PR and obtain all required latest-Head/tested-merge CI plus actual security alert/analysis readback.

No extra unrelated local JVM/browser rerun is required solely by these five files when complete applicable measured runs exist and exact runtime/build/test/frontend/browser/capacity/CI input hashes remain unchanged. Preserve those runs' actual measured Heads; do not pretend they executed on the later canonical tip. Required remote full jobs still run. Any additional runtime/test/config repair needs its affected evidence and final coherent full verification.

Keep the assembler unchanged. Run it only before final manual assessment if needed; its writer intentionally resets to pending. `make governance` runs assembly tests with a temporary ROOT and does not overwrite repository matrices. This preparation introduces no §15 stop condition, no real Provider/shared/production access and no Ready/merge authority.
