# Post-W4 control verification r9

The original receipt preserves the command, source manifests, resource snapshot and outcome. `maven.log.gz` and `raw-reports.tar.gz` preserve original bytes; `archive-index.json` binds compressed and uncompressed hashes. This run does not establish final full verification or CI.
