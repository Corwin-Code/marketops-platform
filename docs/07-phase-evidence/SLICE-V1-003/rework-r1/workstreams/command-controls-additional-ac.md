# Additional command/control AC mapping

This supplement adds 20 previously unmapped ACs to the existing 60-AC shard and strengthens AC006 with exact architecture methods. Accepted text is checked byte-for-byte against the Contract. Source presence is not a test result. The JSON identifies concrete methods, source hashes and per-criterion limits; final current-tree full validation and exact remote CI remain pending.

| AC | Concrete coverage | Remaining evidence |
| --- | --- | --- |
| S3-AC-006 | Only canonical module ports, exact controlled creators and the trusted canonical planner carry authority. | Architecture tests have not been executed by this supplement; parent full architecture gate must bind exact result. |
| S3-AC-009 | Case identity and decision identity retain exact native object, lineage and scoped Profile rather than an estimated variant subject. | Profile eligibility is engineering metadata; real platform verification remains separately gated. |
| S3-AC-010 | Affected identity is retained in the full canonical set and both decision digest and admission consume it. | Full collection/projection tests are jointly owned by source and UI; use final integrated evidence. |
| S3-AC-011 | One candidate acts on one native object and reserves every affected variant; per-variant monetary diagnostics never become separate execution identity. | These methods support object/set integrity; final same-object multi-variant fixture and full suite must supply complete acceptance proof. |
| S3-AC-012 | Native lineage and relations remain metadata, while an unproven independent control level cannot reach a dependent decision. | Queryable hierarchy and both platform UI rendering also require read/browser evidence from the UI stream. |
| S3-AC-013 | Separate profiles retain native identity and amount unit. Status/readback semantics are explicit operation data and not assumed common between providers. | No engineering test establishes real Ozon/Wildberries official write semantics. Real evidence remains unverified/deferred under the separate exact Gate process. |
| S3-AC-014 | Unknown grades and states stay unresolved through calculation, decision and observation. | Final full regression result must include each dependent stage; this is a concrete method mapping. |
| S3-AC-015 | Lineage/mode/current authority participates in the immutable decision digest and current gate comparison; superseded authority is never renewed. | Independent mutations of every native continuity axis require the final fault matrix, not a claim from one policy-drift example. |
| S3-AC-084 | Manual and API reserve the same canonical affected membership and evaluate the same exposure/release functions. | Current Manual9 and Reservation14 are in source r12; full parallel/capacity workload and immutable final identity remain pending. |
| S3-AC-085 | The new provider writer is only AD_BID_CHANGE, alongside the predecessor existing writer. | Non-goals suite and static route registry must run on the final tree. |
| S3-AC-086 | Closed parameter schemas reject adjacent controlled actions; Budget/Status remain governed human instructions. | Manual browser automation exercises this application only; it never drives Provider consoles. |
| S3-AC-087 | Creator and transmission independently check native amounts and complete capability/credential/current scoped authority. | A fictional positive protocol is not production/real provider credential verification. |
| S3-AC-088 | Unverified or unconfigured production authority fails before dispatch; shadow/manual remain explicitly distinct. | Visible Shadow state is supplied by the UI/browser stream and must be integrated with these backend refusal assertions. |
| S3-AC-089 | Protection and optimization are policy directions; exact prior compensation has separate new authority and idempotency. | No real decrease/increase/compensation is performed by engineering verification. |
| S3-AC-190 | Input and evidence guards exclude credential/header leakage; fixtures contain fictional graph IDs and non-resolvable references. | These tests are not an exhaustive audit of Git history or built browser artifacts. Parent secret scan and final artifact inspection are required; no real credentials were read for this workstream. |
| S3-AC-191 | Controlled-write non-goals and disabled production adapters enforce the engineering boundary; the browser harness is an isolated local application. | No actual Provider socket/console was used. Parent must preserve this distinction in browser and CI receipts. |
| S3-AC-192 | Positive ordinary and compensation fixtures assert production_write_enabled=false even after real synthetic human authorization. | Engineering Gate E/EV fixture references confer no actual production or Provider write permission. |
| S3-AC-193 | Applied foundation migrations are preserved and new source is exercised by real PostgreSQL clean installs; the artifact runner refuses unsafe delivery. | Protected-main upgrade, final full Flyway inventory and immutable applied-migration byte comparison are parent full-validation obligations. V0065 is saved and passed isolated migration syntax; integrated tests and final migration/upgrade inventory remain pending. |
| S3-AC-194 | Stable lineage keys, serialized reservation admission, immutable revisions and per-observation replay IDs bound the shared authorities. | Capacity/restart/reconciliation and every fact/task/audit family need the complete parent run; these named methods are supporting slices. |
| S3-AC-196 | The tests mutate real gates, scope/fences and canonical facts/queue triggers rather than only asserting helper return values. | Current transitive Finance/CI/browser additions remain pending until the full measured tree is tested; no Controller conclusion is supplied. |
| S3-AC-168 | Current canonical Settled regression or a conclusive contradiction of primary retained success creates an actual linked Finance Shared Task, while preserving and reopening or escalating the original advertising responsibility. Exact frozen affected membership controls Finance assignment; per-observation history is immutable and replay is idempotent. | Finance workflow tests use migration-role synthetic outcome oracles to test Task/IAM boundaries. The companion source PostgreSQL test derives the financial contradiction from actual canonical facts and invokes the same intake. V0065 fresh SQL passed; integrated result is pending at this receipt. No new profit or Outcome authority is introduced. |

## AC168 integrated result update

FinanceReviewIT 6/6 and FrozenOutcomeIT 10/10 passed in source-r15, including the actual financial contradiction pipeline. The whole batch remains 288 tests / 286 passed / 1 failure / 1 error / 0 skipped. Its durable log SHA-256 is `d1602828ec7dab120c9f47b6a6da8c974ae327d672d8c00dba538b4deb5cf27e`. This is local targeted evidence, not final Head or remote CI approval.
