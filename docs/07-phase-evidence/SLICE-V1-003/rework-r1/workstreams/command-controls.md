# R1 command, identity and control evidence

Scope: Frozen findings 009, 010, 012, 013, 014, 016 and 017, including transitive authority and lifecycle defects discovered by positive-path verification. Parent rework evidence records the exact reviewed Head, immutable Contract/Finding hashes, final publication and CI. This file is engineering evidence; it grants no Gate or production authority.

## Normative takeover

The read-only takeover compared all 20 `PROTECTED_BASE_AUTHORITY` references against exact Git bytes at `08ad7da7d9e75b4ddd1c387a22ac0affba9e1430`, including Product, roadmap, Shared Spine, Baseline, predecessor Contracts/accepted Amendments/acceptance and closure, Owner Decisions, ADRs, governance and traceability. Relevant ADRs 0001–0011, CURRENT_STATE and V1_PRODUCTION_ASSURANCE_MATRIX were read in full. V0040, V0041 and V0042, the current command/worker/ports and boundary tests were read before mutation. The new named Owner Codex authorization governs remote transport; the obsolete Claude local-only entry was not used. No Contract §15 conflict was found in that review.

## Changed authority

- V0058 seals final human approval to the exact immutable selection, independent endorsement, selected Outcome Baseline, candidate, Bundle version, affected set and complete referenced policy/Freshness snapshots. Expiry is the minimum of final-approval lease, human-selected scope, recommendation, required evidence, baseline, finite policy, exact Gate and credential bounds. Reads do not renew it.
- A separate, unconfigured-by-default identity issuer exchanges the verified request principal for a random, one-use, 25-second proof bound to exact purpose, target/version, physical application backend and transaction. Application SQL cannot mint/read proofs, assume the issuer role, write sealed authorizations, or call the removed actor/bundle/expiry command creator. Each consumption rechecks active identity, IdP, authentication revocation and step-up. Arbitrary session GUCs are not identity.
- Historical credential metadata remains `UNVERIFIED`. Independent advertising credential attestations describe exact credential/account/Store scope and finite validity; migrations create no credential, active attestation or secret. The production issuer has no configured URL/password and its bootstrap role is `NOLOGIN`.
- Native bid amounts retain their declared unit. Economic absolute bounds, promotion envelopes and cumulative change use major currency (`CURRENCY_MINOR / 100`) without runtime rounding. Target constraints check exact semantic profile, currency, step, precision, min/max, cause-bound policy and explicit intermediate-protection permission.
- A status-pending native task keeps its provider task key and never returns to APPLY. Retry requires a current fenced readback proving exact prior value/unit/currency and a recorded explicit-not-applied or verified native retry proof within budget. Observation-only polling continues without repurposing expired write approval.
- Exact-prior compensation has a new maker preview, independent Ops Lead endorsement and distinct Owner approval, its own current Bundle/Gate/lease, and a retained reservation. It observes current state first, restores only the captured prior bid, does not overwrite a third value, and confirms restoration separately from ordinary target success. Financial approval disclosure and Maker native-value disclosure are separate role checks.
- Reservation admission is serialized for the organization and exact canonical affected set. Pending recommendations cannot reserve; controlled API admission requires a current sealed authorization. All matching Store, platform and organizational envelopes constrain six axes. The same exposure snapshot supplies the gate and preview; unknown evidence remains unknown. Current official-spend leaves with overlapping windows, mixed currencies, absent mappings or incomplete measurements cannot manufacture capacity.
- The application cannot set release booleans. Release derives configuration proof, unresolved transmission state, the current complete early Completed-sales safety observation, company preservation and every frozen critical unit. Unknown or regression retains the hold. A late regression first creates an affected-set quarantine and only reacquires a previously released reservation if no newer holder overlaps. Reservation changes have append-only history.
- Containment is a true intersection of canonical variant membership, and supports entity, affected set, Store/account capability and exact authority-version scope. Activation permanently invalidates prior approval assets and revokes pre-execution manual packets. Recovery requires independent evidence, technical attestation when applicable, Ops Lead endorsement and distinct Owner approval of new authority; old assets cannot become usable again.
- Bundle content and endorsement history are immutable. Draft, independent endorsement and Owner activation use one-use authenticated purposes, complete validation and exact Gate scope; supersession invalidates old assets. Gate E may not exceed the demonstrated Gate EV object scope. No active Gate, ordinary promotion or generic re-entry permission is seeded by migrations.

## Isolated verification boundary

Owned container: `marketops-s3-r1-sql-20260905`, ID `3c766818172ad760ce74d827f5b13d1c03714cd3a99f6c7e3f8e4a8c353eb501`, image `postgres:18.4`, labels `marketops.owner=codex-slice3-rework-r1` and `marketops.scope=isolated-synthetic`. It has `--network none` and no host port. PostgreSQL is reached only with `docker exec` over its Unix socket. Reset scripts drop only this container's independently created `marketops` database. Existing workstation and other project containers were not touched.

The positive fixture uses a separately named fictional protocol, fake `.invalid` IdP, and non-resolvable synthetic credential reference. It does not promote Ozon/Wildberries evidence or use a Provider socket. `production_write_enabled=false` is asserted. Each Java test gets a random graph. The synthetic issuer is exercised on a separate test database connection; the application role never gains issuer membership.

Commands/results are updated at workstream completion. Intermediate diagnostics in `/tmp/slice3-command-controls-schema.log`, `/tmp/slice3-command-controls-fixture.log`, `/tmp/slice3-sealed-authority-it.log` and `/tmp/slice3-command-unit.log` are local scratch logs, not final CI evidence. Exact final checks and limitations remain pending until the integrated validation entry below is completed.

## Executed integrated control verification

From `backend/marketops-server`, the exclusive (no concurrent Maven, no clean) run was:

```text
./mvnw -q -Dtest=AdBidCommandWorkerTest,AdBidWritePortContractTest,AdBidWriteRefusalTest,AdBidWriteDispatchTest -Dit.test=AdvertisingSealedAuthorityIT,AdvertisingReservationIT,AdvertisingTransmissionBoundaryIT test-compile surefire:test failsafe:integration-test failsafe:verify
```

Result: **88/88 passed**, zero failures/errors/skips: SealedAuthority 15, Reservation 13, TransmissionBoundary 7, Worker 19, port contract 12, dispatch 15, refusal 7. Each PostgreSQL integration class migrated the complete V0001–V0064 sequence. The source log is `/tmp/slice3-command-controls-it-r2.log`; report XML is in the Maven `surefire-reports` / `failsafe-reports` directories and must be preserved with the final run rather than treated as remote CI.

This run includes a fictional three-role final seal/creator, issuer/GUC/session replay attacks, Store-only approval refusing undisclosed Product scope, exact selected baseline, immutable human combined bid/Bundle snapshots, cause-bound protection without conversion/cost evidence, missing danger and unknown critical safety refusal, cause-bound ratio enforcement, minimum expiry and major-unit exposure. Bundle coverage includes real draft/independent endorsement/Owner activation, atomic retirement and invalidation of prior assets, and a Gate edit after endorsement refusing activation. Legacy transmission fixtures now create commands through the real fictional sealed creator before deliberately withdrawing authority; they do not claim an unverified capability can create a command.

Following this run, the compensation integration added the separately tested action-bound human-stop path, restoration exposure and permanent compensation invalidation. Its exact final result is recorded below after completion. These additions do not turn the preceding 88-test result into evidence for unexecuted tests.

Canonical operating documentation was synchronized in the seven approval-expiry, unknown-result, reservation/exposure, kill-switch, quarantine, reenablement and Provider-incident runbooks. Direct control-table DML and fabricated release-condition procedures were removed; the docs now describe authenticated scoped API actions, actual states, evidence checks and fresh authority.

### Compensation targeted verification

```text
./mvnw -q -Dtest=NoTests -Dsurefire.failIfNoSpecifiedTests=false -Dit.test=AdvertisingReservationIT test-compile failsafe:integration-test failsafe:verify
```

Result: **14/14 passed**, zero failures/errors/skips, after a fresh V0001–V0064 migration and complete Java/test compilation. Local source log: `/tmp/slice3-compensation-control-it-r2.log` (2026-09-05, completed after 08:55 Asia/Taipei). The preceding first run found a legitimate fixture error: an exact-compensation Target Policy must have `candidate_count=0`. The fixture was corrected; the schema invariant was retained.

The additional integrated case creates the original fictional command through the real one-use sealed creator, supplies immutable fictional landed/readback history, and invokes an authenticated Operator stop bound by the database to that command. It then publishes the compensation Bundle through three identities, invokes a new Maker preview, separate Ops Lead endorsement and distinct Owner approval, observes an empty compensation gate, leases compensation and opens a real `RESTORE` attempt for the captured prior Bid. Maker self-endorsement and endorser final approval are refused. Credential evidence revocation followed by restoration still leaves the old compensation permanently invalidated. The test asserts `production_write_enabled=false`; it performs no HTTP call or actual Provider write.

The related same-class corrections include: only latest matched amount/currency/unit proves current ownership; an action-bound Stop or current latest Regression is required rather than a reservation boolean; compensation contributes a second absolute Bid movement to aggregate exposure; its exact Gate command budget is enforced; and append-only compensation invalidation covers referenced IAM, credential, policy, Bundle, Gate, switch and containment changes. The original sealed action also retains permanent invalidation after credential evidence changes.

These completed targeted runs preserve the tested control behavior at their execution time. Subsequent baseline and materiality corrections require a current integrated rerun. Final repository-wide checks, performance/browser evidence, immutable normative hash checks, append-only publication and exact remote CI evidence are recorded by the parent R1 handoff; no local test is represented as independent Controller approval, Ready, merge or production authorization.

### Canonical baseline and materiality dependency correction

The integrated source review found that a `COMPLETE` label and arbitrary baseline JSON could bypass the trusted planner. V0059 now removes application INSERT authority for baseline, stage and critical-unit rows. The canonical planner uses the existing independent, unconfigured-by-default issuer to attest its complete computed payload, exact baseline/organization and physical application transaction. A controlled atomic freeze consumes that one-use proof and records immutable provenance. The current validator compares the whole attested payload, exact typed Owner policy, three distinct windows, complete unit membership and current purpose Freshness authority. It introduces no second profit calculator and no real credential.

V0058 calls the same canonical validator at final approval, command creation and every live write gate. The shared synthetic fixture now contains explicit typed Owner thresholds, actual synthetic Completed Sales and source coverage, three complete stage structures and an explicitly synthetic migration-role trusted Planner attestation. The application cannot manufacture that attestation.

The isolated current V0001–V0064 migration and upgraded fixture passed on 2026-09-05. A subsequent actual application-role query returned `canonical=true`, `attested=true`, `app_direct_insert=false` after correcting a JSON-operator precedence error. `/tmp/slice3-canonical-baseline-authority-probe.log` records that limited SQL probe; it does not replace the pending full Human/Manual/Outcome/command rerun.

The seal, creator and write gate also consume the unified `ops.ad_materiality_assessment(bundle,candidate)` route. A missing or unresolved route cannot be sealed, and a route that differs from the immutable sealed route blocks current execution. The assessment itself and one-factor materiality tests are in the parent workstream. The initial all-Material policy remains in force.

`command-controls-traceability.json` and its Markdown companion contain all seven owned Frozen Findings and all 60 distinct associated ACs, with exact source/test references, preserved targeted logs and explicit pending verification. `ci-gates-review.json` and its Markdown companion enumerate the exact 12 repository checks and their publication/isolation boundaries.

### Current coherent targeted superset

Source run `source-r12` completed at 2026-09-05 09:53:38 Asia/Taipei: **248/248 passed**, zero failures/errors/skips. It includes the real trusted planner, Human10, Manual9, FrozenOutcome7, Canonical10, SealedAuthority15, Reservation14, OrdinaryApproval7 and Materiality13, plus the named domain suites. Its exact Maven command and report identity are in `command-controls-traceability.json`; durable log `logs/slice3-frozen-outcome-pg-r12.log`, SHA-256 `95a709092538526bf914e2a0a9cbf0a384e194397499cec3d06fa5c032c7ba22`.

The Ordinary positive uses the real Maker → independent Ops endorsement → same Ops final approval → command path and a trusted planner proof. Bundle promotion references are frozen at the initial INSERT, with the reciprocal FK deferred only to transaction commit. Orphan commit, missing Owner evidence, inactive promotion and later content mutation are rejected. Cause-bound fixture transformation now asserts its exact source template; the conservative MaxCPC headroom predicate remains enforced.

This result precedes the additive Finance review V0065 correction. Source-r15 subsequently passed FinanceReview6 and FrozenOutcome10, including three actual canonical financial pipelines. The whole r15 batch remains PARTIAL_FAILURE: 288 tests, 286 passed, one failure and one error; its durable log SHA-256 is `d1602828ec7dab120c9f47b6a6da8c974ae327d672d8c00dba538b4deb5cf27e`. Final repository-wide/remote verification remains pending. The additional AC shard maps 20 new criteria and strengthens AC006, producing 80 unique criteria with the original shard; a source/test mapping is not independent acceptance or Controller approval.


### Final same-class ingress and old-suite repair

A real `pg_proc`/`has_function_privilege` inventory found the unused V0050 caller-role regression reopen function still executable by the application. V0058 now precisely revokes that entry while retaining canonical `activate_ad_regression_containment(observation_id)`. No historical migration body is changed by this correction. The privilege suite asserts actual SQLSTATE 42501 for both the old reopen route and the removed caller-boolean reservation observer, all private authority tables without app DML, and the exact 73 non-trigger callable signatures.

The eight original adversarial Gate scenarios now each create their own fictional command through the real issuer proof, final seal and creator. App privilege and row-shape refusal are explicitly distinct from live gate evidence. Kill and envelope restoration can resolve their named current axis but cannot erase the append-only invalidation or reuse the old final approval. These replacements and the narrow revoke were applied only after the UI build window closed. Isolated SQL probes passed; source-r16 first failed test compilation, r17 exposed five precise assertion failures, and the corrected W1 r18 Gate8/Privilege10 suites passed. The r18 batch remains PARTIAL_FAILURE due to one Vertical error.

`command-controls-old-suite-probe.json` retains the five exact preparation logs, including two scratch-script failures that were corrected without weakening a control. `command-controls-old-suite-scan.json` distinguishes confirmed obsolete assumptions from tests that simply still require full execution. `command-controls-fault-seeding.json` records 50 exact faults and six positive controls, separating early schema/privilege refusal, actual authority/gate boundaries and fake-port worker evidence. The accepted Contract and Frozen Finding SHA-256 identities remain unchanged.

## W1 r18 control verification

Exact W1 `60638b1fc1a227b50f4b3ede1ba0bb983407bfdc` passed Gate **8/8**, Privilege **10/10** and Transmission **7/7** in source-r18. Its whole batch remains **27 tests / 26 passed / 0 failures / 1 error / 0 skipped**: the Vertical positive journey lacked a proven-loss Case. The original failure log is preserved with SHA-256 `7daf4fff0f738e727f74dbdc81044351032e13db68b6c63ca601d224c0501b6c`. `command-controls-r18-receipt.json` contains the exact command, time, execution scope and limitations. This result does not replace full clean verification or final remote CI.

## Post-W4 control closure status

The post-W4 F014 reader correction is measured by `f014-post-w4/r8/receipt.json` (238 actual cases, all passing) and the additional actual recovery chain by `f014-post-w4/r9/receipt.json` (31 actual cases, all passing). Earlier failed attempts remain in the same archive. These receipts bind their own source snapshots; they do not verify the subsequent changes below.

Associated spend sums complete official report amounts intersecting the governed measurement interval. A report crossing the left endpoint is included whole, conservatively, without fabricated proration. Future, incomplete and overlapping/conflicting reports cannot supply known spend. The retained-sales exposure axis separately requires canonical company and affected-unit coverage in the existing Owner-defined window; absent listing/Store evidence remains UNKNOWN/null. The cumulative bid-change window remains a separate Owner policy field. The 33 aggregate PG cases exercise each independent limit, unknown inputs and actual concurrent reservations; they are not the materiality axis suite.

The new shared isolation reader consumes actual Shared PriceCommand and existing comparable price/currency/promotion/sellability/availability facts. A known material overlap is checked at preview, seal, API/manual admission, creator and final APPLY. Unknown-but-unchanged context remains explicit uncertainty, without inventing a known intervention. Source contributes 21 cross-domain cases plus two Manual cases; their new execution evidence is pending.

Authority-version quarantine now has a dedicated signed human route and one-use invocation purpose. It requires exact organization-wide Owner/Ops control authority and an organization-wide Ops reviewer. A Store grant is insufficient. Version consumers are associated with the exact object through Gate allowlists, frozen candidate authorization or Manual references; an unrelated current Profile/Bundle in the same Store is excluded. The 11 new test invocations include actual HTTP/SQL negative boundaries and two-Store recovery through real three-person Bundle publication and actual attestation proofs. Synthetic Owner policy/Gate rows are inputs, not claims that production evidence was accepted.

Organization-wide recovery cannot close while any active Bundle still uses the rejected version. All recovery attestations must retain the corresponding organization-wide role/scope; the final Owner remains distinct from the stopper and Ops endorser. The replacement Bundle parameter is a new authority anchor, not authorization to overlook other Stores. Closing review does not permit the rejected version to be activated again and does not revive old approvals. Technical causes retain their additional security requirement.

The real-worker retry supplement preserves an already addressable native Task through inconclusive status polling, then uses resolved status and independent readback before an allowed retry. Initial unaddressable APPLY timeout remains Unknown. Four new actual-worker PG cases supplement the 18 SQL retry cases; no Provider request is involved.

Gate E publication now checks exact demonstrated native pairs and maximum change, plus the same exposure scope/currency/measurement basis with all six axes non-expanding. Its new Owner-defined pilot window is distinct from the historical Gate EV observation window. The new publication matrix remains separate from the eight current Gate mutations and eight endorsed Bundle mutations.

All new controls above are `IMPLEMENTED_PENDING_CURRENT_UNIFIED_TARGETED_FINAL_UNSELECTED_FULL_AND_EXACT_CI`. The complete R17 attempt failed during isolated Ryuk startup before business assertions; it is not proof of a source defect or a pass. The R18 retry reached migration loading and failed on a SQL alias syntax error; its exact original resource bytes remain in the run archive. A narrow migration validation retry and then the complete affected set remain pending. Current engineering reasoning is recorded in the shard's `postW4EngineeringReview`; no Controller verdict, Ready state, merge, Provider verification or production enabling is asserted.

The complete affected R20 run finished with 262 actual nodes, 7 failures and 14 errors; its source remained stable. AuthorityVersionStop produced eight passing invocations and three fixture errors before the positive stop/recovery assertions. Those three failed because cloned PENDING Bundles retained empty failure codes, violating the explicit validation-state check. A test-only correction supplies `NOT_VALIDATED` while pending and clears it only on the validated transition; the eleven invocations still require a current rerun. The 51-criterion draft indexes individual R20 testcase nodes while preserving the failed whole-run status and current-source byte mismatch.

R21 subsequently measured all twelve AuthorityVersionStop cases, GateMatrix25, RetryWorker9, CrossDomain21, Reservation39, Privilege10, ActorRevocation14, RetryProof18 and OperationsRead10 passing. The whole 289-node batch remained failed because `AdBidWritePathIT.absentBundleReportsItself` exposed an extra semantic refusal on a missing Bundle. Root restored the missing-Bundle early return; the separately bound R22 run passed all 83 nodes in AdBidWritePath21, SealedAuthority15, AuthorityVersionStop12, Privilege10 and GateMatrix25. Both runs are retained under `final-controls/r21` and `final-controls/r22` with raw XML archive members, hashes and source manifests. Formal shard fields now cite those exact measurements; final unselected clean/full verification and exact PR CI remain pending.
