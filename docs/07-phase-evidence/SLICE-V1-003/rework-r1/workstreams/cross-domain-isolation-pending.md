# AC131 / §6.21 implementation receipt (R21 scoped pass; final validation pending)

The implementation reads existing Shared facts, mappings and real `ops.price_command` records. It creates no new materiality policy, fact source, scheduled-intervention registry, Price writer or cross-domain serializer. A review Task or recommendation with no PriceCommand consumes no isolation capacity.

`ops.ad_action_isolation_snapshot(affectedSet, baseline, asOf)` returns independent `failures` and `uncertainties`. Known nonterminal PriceCommands overlapping exact frozen Listings or current mapped Internal Variants yield `KNOWN_CROSS_DOMAIN_INTERVENTION`. Frozen-known effective price/currency/promotion/sellability/positive-zero availability that differ from currently accepted canonical facts yield `CROSS_DOMAIN_BASELINE_NOT_COMPARABLE`. These are the existing comparable context factors; native status and content completeness are displayed but do not invent a material threshold.

Missing context produces `CROSS_DOMAIN_CONTEXT_UNRESOLVED` only in `uncertainties` and an `UNRESOLVED` state. It cannot claim `ISOLATED`, cannot hide a different known factor change, and does not itself invent a new action blocker. Existing canonical baseline/freshness/evidence/Outcome completeness gates remain independently mandatory.

The same failure predicate is consumed by API final seal, reservation admission, public command creation, and the live write gate. Decision projection/action blockers and execution unresolved reasons consume it; the scoped Impact Preview presents the full snapshot. Human final approval independently checks the action blockers. Manual proposal current/select/endorse/approve/start use the same SQL predicate. Exact prior-bid compensation remains on its existing original-reservation recovery path.

Historical knowledge filters both observed time and canonical accepted/source time, and ignores a future accepted correction when reconstructing pre-action context. Command state reflects the actual committed current Shared state. Price creation does not acquire the ad advisory lock: final gate rereads committed known changes, but this is not a claim of fully serialized cross-domain writers. Unexpected changes after transmission continue through existing Outcome confounding and safety handling.

## Named assertions and recorded R20/R21 results

`AdvertisingCrossDomainIsolationIT` contains 21 JUnit invocations: exact overlapping command at preview/seal; approved unexecuted recommendation; same-store different known Internal Variant; new mapping to affected Variant; foreign organization; 4 Shared terminal states; newly committed command between approval/reservation, reservation/creation, and creation/APPLY gate; unknown context without invented materiality; 5 independent existing comparable context factors; 2 descriptive-only factors; unknown factor plus different known change; late accepted/future fact knowledge boundary. R20 executed all 21: 20 passed and the COMPENSATED terminal fixture errored before reaching its non-blocking assertion. R21 then executed all 21 against the repaired source with zero failure/error/skip. The complete Manual15 suite, including both named isolation checks, also passed.

`AdvertisingManualWorkflowIT` includes two actual existing planner/human lifecycle checks, both passed within R20 Manual15: a Shared command appearing after selection refuses endorsement, and one appearing after final approval refuses start with no reservation or API command.

`AdvertisingCrossDomainPriceSeed` is an explicitly synthetic, migration-role fixture of real Shared schema: one canonical observed-price Metric with definition/input identity, active typed economics profile, eight family contracts/components and feed watermarks, commercial policy with input-age bound, real approval/execution guardrail snapshots, approval, and optionally a real PriceCommand. Existing binding triggers stay active. It changes no global write flag and calls no Provider. It proves DB consumer behavior, not an independently exercised Price human UI journey or Provider execution.

The private context helper is non-executable to the application role; the two public reader functions are added to the exact sanctioned signatures. Full/CI status remains pending exact final source.

## R20 failure, repair and current proof boundary

R20 was an overall failed run: 262 test nodes, 7 failures, 14 errors, no skips. Its exact working source was stable during the run; this was not the clean W4 source. The immutable run receipt is `final-controls/r20/receipt.json`, with original log SHA-256 `265777d96bcbd631e40ce24d98afe210eeeef3bc4bc9fa8f4234b20b3c66c29f`. CrossDomain's raw XML SHA-256 is `52b33c1442d8cb7bb5ac12ce82403d6e23363bb26ec0b2e0f6d7aa3f4cab6272`; Manual's is `391cef535a378bc0de345f1ba0e1e1b199d1b54d83ee7c59e720157b41d6476d`. Raw XML is retained in that run's archive. Passing methods do not turn the overall run green.

`terminalSharedCommandsDoNotRemainPermanentIsolationLocks(String)[3]` originally proposed `COMPENSATED` without an observed restore. The protected Shared Price trigger correctly refused that claim. The fixture now supplies synthetic historical accepted RESTORE followed by a later exact prior-value READBACK, bound to the same command/fence and Raw response identities. It retains the one-time attempt completion trigger and compensation observation check, then executes the unchanged empty-isolation and actual ad command-creation assertions. This proves the cross-domain consumer's treatment of a legally observed terminal Shared state; it does not claim an executed Price worker or Provider journey.

The formal cross-stream entry in `facts-outcome-traceability.json` retains the R20 failed-run history and now binds the passing scoped R21 assertions to the exact archived source manifest. A new clean final-source run, remote CI and Controller adjudication remain pending; no earlier W4 or r8 result is inherited as validation for these changes.

## R21 scoped result and exact source binding

R21 completed with 289 actual test nodes, one failure, zero errors and zero skips. `AdvertisingCrossDomainIsolationIT` passed all 21 invocations; `AdvertisingManualWorkflowIT` passed all 15, including both isolation methods. `AdvertisingRetryWorkerIT` also passed its complete nine-case real-worker/repository set. The whole run remains a failed batch because the existing `AdBidWritePathIT.absentBundleReportsItself` assertion received an additional semantic failure reason. The later root early-return repair and its R22 run are outside this R21 proof.

The archived receipt is `final-controls/r21/receipt.json` (SHA-256 `3da1c8e3add28f576d69a30b31b99a6ccbc5bee9ba38043c75db916d8830438f`). Before/after source manifests are identical at `749f08b59aea5c8cc1d27bd99be025e2ba29f94d8a39c355af576fa667a1a112`; all source hashes in the formal contribution are taken from that manifest, not the subsequent working tree. The original log SHA-256 is `e59b91e961e86d88bf638252aede411f282f99247eaca0708b83108b44817ed3`.

Raw reports are retained in `final-controls/r21/raw-reports.tar.gz`, with member names and hashes in `archive-index.json`. CrossDomain XML SHA-256 is `49dc05a4b4194d0039b6cef0fe300ebdc52873ce2f287ed3bdfb62bb8e2bd8aa`; Manual is `cef975a57f3c43338c3aff59ca21aaf9cdaaa28a7ef26cbda819c20a3a45fb12`; RetryWorker is `406ed60200a7fc25df75c1bd036a723219a1dd297459937243953987bda06956`. Named nodes were checked for actual failure/error/skipped children. These scoped passes are engineering evidence for the repaired boundaries, not final all-finding verification or a Controller verdict.
