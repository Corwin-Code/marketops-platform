W8 completed evidence publication staging. Original receipts are preserved unchanged, including their original temporary paths and failed attempts. This staging does not re-identify evidence as a later commit, approve the Controller verdict, or close the pending W9 UI correction.

Every original source is mapped to its archive/member and uncompressed SHA-256 in source-archive-member-manifest.json. Full-run JARs remain outside Git with their measured hash/source stamp. The sole designated signed-URL stderr is excluded; its original hash and the existing sanitized derivative are retained.

Git text filters, especially CRLF normalization, can change raw text bytes even without a content edit. Raw logs, XML, scripts and original CRLF text are therefore retained inside tar.gz/ZIP; readable direct JSON is copied unchanged only if it has no CRLF. Do not unpack raw archives into Git or rewrite receipt paths. Before/after Git publication must still hash the archive blobs and direct copies against this staging manifest.

The original security scan remains false for eight static TypeScript CodeQL rule-help credential-shape occurrences. Exact original SARIF, prior reviewed static-help proof and narrow triage remain together; the scanner patterns are not weakened. Recursive publication scanning must inspect tar members, nested ZIP members and their text.

The first complete recursive scan additionally found a signed URL in the frontend first-download stderr. That second private original is now also excluded. publication-private-output-repair.json binds the original private hash, sanitized derivative, rejected first archive and safe replacement archive without modifying the original source or first scan.
