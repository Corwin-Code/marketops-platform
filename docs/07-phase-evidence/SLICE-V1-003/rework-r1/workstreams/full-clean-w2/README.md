W2 full clean verification failed in Surefire: 1544 tests, 3 failures, zero errors/skips. Failsafe, packaging, coverage report and advertising capacity did not run. The source remained clean and unchanged throughout.

Original commands, timestamps, log and before/after source identity are retained. `raw-test-artifacts.tar.gz` contains all 116 original XML reports and the produced JaCoCo execution bytes; `archive-artifact-index.json` preserves uncompressed SHA-256 identities. No JAR was produced or represented as verified.

The resource receipt was previously collected through the approved host/runtime check and is copied here as `runtime-resources.json`; the inline sysctl probe in this sandbox returned exit 1, as preserved in the original run receipt. Host and Docker resource values are kept separate.
