# W8 controls preparation — full result pending

The 51 exact control criteria and seven owned Frozen findings retain individual reasons and positive/refusal boundaries. Original W6 measurements are referenced without changing their Head, tree or results. W8 source is read independently; `source-delta-review.json` explains the three upstream time fixes and changed test fixtures rather than mechanically replacing hashes.

R25 is actual 72/72 selected complete-class evidence on a precommit working tree with W7 build metadata. Its source manifest is compared to W8 Git bytes. The two new F013 compensation Gate-scope parameters actually pass after real app approval and RESTORE lease: revoked Gate or removed exact object causes MO092/COMPENSATION_GATE_SCOPE_ABSENT, zero RESTORE attempts and unchanged candidate/approval. Original R24 Raw-uniqueness errors are retained.

`named-proof-plan.json` records exact methods, W8 source hashes and separately bound R25 nodes; W8 full case arrays remain null until Root supplies the completed frozen run. The real earliest-expiry tests use lawful 900-second approvals originating 885 seconds earlier and actual roughly 15-second waiting, not fake-clock time travel.

The five-file canonical patch is currently mechanically applicable by actual read-only `git apply --check`; it was not applied. The later tip must be classified **NON_RUNTIME_CANONICAL_GOVERNANCE_AND_EVIDENCE_ONLY**, not simply documentation-only: CURRENT_STATE, two Python validators, two Python test files and evidence/docs change. Backend/frontend runtime, their tests, migrations, build and workflow inputs must be proven unchanged. Actual new governance execution (expected 412 only if discovery remains baseline 410 + 2) and all latest final-tip remote CI remain required. W8 full must never be restamped as a run on that tip.

AC200 deterministic engineering validation and independent Controller no-unresolved BLOCKER/MAJOR assessment are separate pending obligations. All 24 real-world release scopes stay production blocking. No repository edits, Maven, database, Provider, environment changes or new test execution occurred in this preparation.
