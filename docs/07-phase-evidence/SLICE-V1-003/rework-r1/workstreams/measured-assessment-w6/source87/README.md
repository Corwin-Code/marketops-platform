# Measured individual source87 engineering review

`source87-individual-measured-review.json` retains all 87 exact accepted criteria, each individually written engineering reason, positive/normal assertions, negative/adverse assertions and original proof limits. It binds the named Java methods to completed W6 source and frozen JUnit cases. It is an input for Parent's final individual assessment, not a Controller verdict or automatic 87-criterion PASS.

The exact full run is W6 `3ed3f4c87c336cb07188e470528f328358fb279f`: 2472 observed testcase nodes, no failures/errors/skips, stable source and clean start/end. The 2471 suite-declared total and the single nested shape-test 12-versus-13 declaration mismatch are retained separately. Total-suite success is contextual; each criterion's scope is justified by its own specific assertions and reasoning.

The 87 criteria reference 149 unique Java methods. Including the existing AC131 and F012 contributions, the review independently verifies 173 unique method bindings, 191 unique actual case nodes and 31 frozen XML files. Every bound case's original SHA, suite/testcase ordinal, exact class/name and absent failure/error/skipped children was checked, together with source bytes against the completed source manifest. One existing supplemental method, `descriptiveListingFieldsDoNotInventNewMaterialityThresholds`, is absent from the central AC index but has two passing exact parameterized cases in the frozen CrossDomain report; it is identified as direct supplemental evidence and is not mislabeled as one of the central 364 references.

`source-method-xml-bindings.json`, `source-identities.json` and `independent-frozen-xml-check.json` preserve the exact evidence. The measured source is read from immutable W6 Git objects and completed `/tmp` artifacts; no live target report, source mutation, Maven run or external Provider was involved. The earlier draft remains unchanged.

W6→W7 exact input-delta evidence is referenced without renaming the local run as W7. Parent retains final W7 backend CI/provenance, UI/control/root contributions, artifact/Git/governance reconciliation and independent Controller judgment. AC199 deliberately has no arbitrary Java surrogate. No new missing named backend assertion or execution failure was identified in this bounded existing scope; broader acceptance remains an individual engineering decision.
