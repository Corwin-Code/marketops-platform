# R1 responsibility and human decisions — work in progress

This is an implementation work log, not a closure verdict, Gate evidence or production enablement. The accepted Contract and Frozen Finding Set remain unchanged. `production_write_enabled=false` remains in force. All runtime exercises in this workstream use newly created isolated PostgreSQL instances and fictional data; no Provider transmission is invoked.

Findings addressed: F006 (responsibility, response and accepted risk), F008 (Maker/Operations/Owner chain), F011 (finite targets and exact Impact Preview). Exact finding identifiers and final closure evidence remain in the central finding matrix.

The cause owns one existing-authority Task through `ops.ad_case_responsibility`, independently of whether an executable candidate is available. A configured lane-specific SLO and reporting calendar are frozen with the original first-raised timestamp. View, acknowledgement, attributable action, completion, reopening and escalation remain distinct journal events. Canonical record identifiers are required for actions; free text cannot prove a repair, approval or configuration outcome. Assignment requires the target person's live cause-owner role and full resource grants.

Accepted risk binds platform/account/store/object, the complete affected set, profile, current cause and lane, both profit axes, official Spend, sales evidence, variant sellability/availability, policy identity, period and mandatory review. Requests, Operations endorsement and Owner decisions retain their actor and reason. Only Action time can pause. The effective authority cannot outlive any selected covering role/grant or the accepted period. Identity, Case scope/cause/recurrence and referenced Owner policy changes leave permanent evidence even if the old value is restored. Known action intent and risk acceptance cannot coexist. Invalidation cancels earlier candidate recommendations and reopens the same Task with an append-only event.

A Marketplace Operator selects one deterministic, normalized candidate. A distinct Operations Lead endorses the exact authority; the initial material route requires a third person, the Owner, for final per-command approval. Candidate parameters retain the established three-key contract. The selected Outcome baseline is explicitly bound to the human chain and the final command. Generic recommendation transitions cannot advance an advertising decision.

The preview records a disclosure-controlled snapshot alongside its guardrail evaluation: exact Provider amount and unit/grid, economic and native-unit Max CPC, both current profit axes, affected variants and critical sales units, frozen evaluation plan, purpose freshness, Metric/Policy/Profile/Bundle versions, aggregate exposure from the same six-axis gate authority, reservations and uncertainty. Missing numerical values remain absent. Organization-wide financial evidence is required to view the company-wide baseline and aggregate exposure.

Target generation applies absolute economic bounds after converting them to the Provider's native money unit. Provider rounding cannot cross policy or economic limits. Multiple deterministic alternatives are generated only up to the policy count, deduplicated after normalization. A Protection intermediate target requires explicit policy permission and is presented as recovery still in progress. A cause-bound decrease has no profitability or health claim. Its financial uncertainty remains visible alongside separately computed action-blocking reasons; only an exact closed set of irrelevant financial defects can be excluded for the two proved one-sided causes. The SQL seal, command creator and execution gate independently enforce the same rule.

Verification in progress:

- `StaffedResponseClockTest`: six golden boundary cases passed, including partial minutes, overnight coverage, weekends, repeated DST hour and Action-only pause.
- `BidCandidateSetTest`: five deterministic and unit-conversion cases passed in the source workstream's targeted runs.
- `AdvertisingHumanWorkflowIT` eight real-service PostgreSQL scenarios passed together with 47 finite-target, Proposal, Guardrail and staffed-clock tests: `./mvnw -B -ntp -Dtest=AdvertisingHumanWorkflowIT,AdvertisingProposalServiceTest,AdBidGuardrailTest,BidCandidateSetTest,StaffedResponseClockTest test`, 55 tests, zero failures/errors/skips, 2026-09-05T08:39:35+08:00, 33.003 seconds (`/tmp/slice3-human-workflow-it-r4.log`). These cover the actual three-person decision chain and command creation, one baseline across selection/seal/command, exact financial preview, cause-qualified financial unknowns, missing/stale/duplicate danger evidence, unknown critical safety refusal, risk/action exclusivity, view versus acknowledgement versus action, live assignment scope, immutable expired approval, single expiry count, same Task after restored authority invalidation, and one escalation for unresolved coverage. The subsequent missing-profile repair scenario is added but not yet verified; final exact-source full verification remains required.
- A later source-workstream run verified all nine `AdvertisingHumanWorkflowIT` cases, including missing-profile recovery and historical unresolved clocks. The containing r7 command had two unrelated Manual fixture errors and is recorded as a mixed failed run, not an overall pass; those five Manual cases passed separately in r8. Exact commands are in `outcome.md`.
- The tenth PostgreSQL scenario adds four concurrent intakes for one independent cause on the same native object, one resulting Task, three real generated/persisted candidate Recommendations and idempotent replay without any reservation. It also asserts that a responsibility binding recorded after the query cutoff cannot affect a historical SLO. This individual test passed in the UI r5 mixed run, whose authority-dependent tests failed during an incomplete cross-migration snapshot; the full coherent batch remains required.
- `make governance` passed with 398 Python tests in `/tmp/slice3-r1-governance-preflight-r4.log`. The migration validator admits only the exact V0057–V0064 inventory and the complete V0064 three-index replacement; omitted non-advertising states or candidate/Case keys are negative-tested.
- The full backend, frontend, rendered browser, capacity, migration/privilege, artifact and remote CI checks remain required before closure. The source/control workstreams are hardening the trusted frozen-baseline insertion boundary; it is not yet recorded as closed.

Current R1 follow-through (2026-09-05): the later r30 batch passed all 46 Proposal/Human/Vertical tests, including four actionable responsibility routes, Watch without a Task, actual concurrent calculation and dual-role self-endorsement refusal. Target generation and the real impact snapshot now compare intermediate Protection targets against the native-unit ceiling after the exact policy headroom; raw Max CPC cannot prematurely label recovery complete. The r31 batch passed all 14 responsibility/disclosure tests, including successful reassignment with preserved timestamps/journal prefix, five independent SLO strength refusals and outside-staffed-coverage continuing harm.

The semantic mapping review retained its initial findings and prompted exact per-criterion assertions. The subsequent r32 run was mixed (97 tests: 3 failures, 2 errors, 0 skips): all nine Responsibility and 15 Sealed Authority cases passed, including distinct cause owners/canonical links and actual cause-bound Preview absence of numerical Max CPC. The added Human preview and exception assertions exposed incomplete synthetic fixture setup (no explicit critical unit, no Case bundle), not a successful proof; those fixtures were corrected before rerun. r33 then stopped at test compilation because the new synthetic browser-token helper omitted its checked-exception declaration. These failed runs are preserved in the raw logs; they are not represented as full verification. Final clean committed-source verification remains required.

The corrected r34 complete selected-class rerun passed all 97 tests with zero failures, errors or skips (`workstreams/final-targeted-r34.json`), including all 16 Human cases and the complete-field/critical-unit Preview, exact accepted-risk snapshot, out-of-set/rejected choice and unresolved final approval assertions. This closes the preceding targeted fixture failures; it remains a working-tree measurement before the clean checkpoint.
