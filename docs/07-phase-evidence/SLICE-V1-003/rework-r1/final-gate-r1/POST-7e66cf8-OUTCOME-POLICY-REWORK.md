# Outcome authority and Optimization qualification rework after 7e66cf8

This checkpoint continues the same authorized R1 and Final Closure Verification scope. It repairs the current Outcome Policy authority and a same-class Optimization reachability defect. It does not change the accepted Contract, Frozen Finding Set, Owner thresholds or production enablement. Independent Controller approval and full final verification remain pending.

V0072 supplies one applicable Outcome Policy resolution for calculation, Preview, candidate and Manual selection, and new frozen-baseline preparation. Geographic and cause specificity use a partial order: a Store-generic policy does not automatically override an organization-exact-cause policy. Multiple maximal policies return OUTCOME_POLICY_CONFLICTED; an absent or incomplete unique maximum returns OUTCOME_POLICY_UNRESOLVED, with no broader fallback or date/ID tie-break. Current selection also requires the exact bound Policy. Queue and proven-harm responsibility remain visible, while otherwise qualified Optimization becomes owned policy-repair work. A completed action still consumes its original immutable Policy and frozen thresholds after a newer policy or conflict appears.

The actual consumers expose the exact blocker through the existing conflict response. Manual options retain their own blockers, so one eligible option cannot authorize another blocked option. Unknown blocker codes remain visible, malformed responses fail closed, and old payloads without per-option authority remain blocked. Rejected selection appends no selection, baseline, reservation or command. V0072 preserves the existing baseline authority, app privileges and historical migrations; the six current migration oracles now include the append-only V0072 boundary.

The previous recoverable-value formula was positive only above Max CPC, whereas Increase required a Bid below Max CPC. A positive Task materiality threshold therefore made a real Increase unreachable. Under-ceiling value now measures static economic space in the same observed cohort: max(0, min(N * C, A) - N * b), floored once to Money precision. It uses the existing qualified traffic, write-grade Max CPC and original allowable-spend authority, without forecasting new traffic, sales or profit. Above-ceiling diagnostic numbers remain unchanged. Currency mismatch produces no value, and every branch preserves the weakest actually consumed evidence grade. Every qualification tier and current period applies its own minimum recoverable amount and confidence; a lower Task threshold cannot supply bid-write eligibility.

The new isolated application/PostgreSQL tests start from explicit synthetic business facts and Owner topology/policy inputs. They derive the current Metric, Case, Task, candidate, Maker selection and attested canonical baseline. The positive observed cohort has 500 traffic, Bid 20, Max CPC 25, allowable spend 12500 and economic space 2500. Both original Task/write minima remain 1000. Original Target controls remain count 1, relative limit 0.5, absolute limit 50 and headroom 0.01; the conservative endpoint 24.75 normalizes on the existing 0.5 grid to 24.5; the finite candidate set persists both its request and normalized target as 24.5. The missing/conflicted negatives first generate the actual candidate, then change only Outcome Policy and require exact refusal with no new baseline. Prior sustained-period records, Owner Bundle/Gate and pre-action mapping/context are declared synthetic inputs; this proof ends at selection/Planner and does not claim human final approval, command transmission, Provider access or APPLY throughput.

Historical source 7e66cf87afc15773d4f6e6e6717a86e7b8336ae5 completed its unfiltered local backend run with 2644 passing nodes and exact successful CI. Those results predate these repairs and remain historical checkpoint evidence. Original failed diagnostics are retained with their exact source identities and cannot be substituted for the new checkpoint. The first empty-settings headroom attempt stopped before Maven clean/tests when the sandbox refused a public dependency-cache metadata update; its 74 collected old nodes are explicitly preexisting, with zero newly executed nodes. The next complete unit run produced 1629 passes and one failure: an old under-ceiling zero oracle encoded the repaired defect. Its unchanged N1000/C10/A10000/b5 fixture now asserts the independently calculated 5000 and retains the separate above-ceiling diagnostic test.

The subsequent diagnostic ran all 1630 unit nodes successfully and 99 focused PostgreSQL nodes, of which 96 passed and the three new Increase nodes failed on the test’s request-field oracle before selection. The actual finite candidate set starts from the normalized endpoint, so its stored request is 24.5, not the intermediate 24.75 endpoint. Only that test oracle was corrected. The final focused rerun completed 33 headroom unit nodes and all five Optimization PostgreSQL nodes with zero failures, errors or skips in 35.880 seconds, on dirty source inventory `0270adc39585c868a5f6993b8b35324a2bd3b5addf3ebc6c67485269d43a061f`. Its actual positive selection and both exact Policy refusals passed. These are distinct diagnostics, not a summed full-regression total or a final clean-source PASS.

The checked-in method-binding tools only propose exact method-to-raw-node mappings. Their independent synthetic checks and Git input hashes do not close a product acceptance criterion. All 200 criteria, 22 Findings, 115 clauses and CV-A–CV-E require current source review and actual positive/adverse evidence admission after the nine complete layers. The assessed source checkpoint and later evidence-containing commit retain separate Head/tree/CI identities. PR 30 stays Draft, production_write_enabled=false, and AC200 awaits independent Controller review.
