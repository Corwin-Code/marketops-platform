# Verification corrections after source checkpoint 98344bb

The local unfiltered run on clean source `98344bb42025b300ae1265faccd232d9ed5437fc`, tree `565c2e052abd0eaf35d72b5c7b3e0a3c28f834ad`, produced three completed FlywayMigrationIT failures. Its separate approved-file list still ended at V0070. Source membership, database history and contaminated-database recovery all consume that same list; the actual 71 migrations had applied. The earlier migration scan missed this independent list.

Root stopped only the identified owned Maven and test JVM after the failures were known. The collector retained 1,967 passing nodes and three failures with exit 143 and stable source inventory `94ec04852bf9587a82299662bdf7118716bc433f41a727a27bf3844626a2742f`. This is an interrupted failed run, not completed full verification. RepresentativePerformanceIT was interrupted; remaining suites and the advertising capacity tests were not measured in this run. The original reports, command log and explicit failed-partial disposition are retained separately.

The sole migration-test change appends the exact V0071 filename to that independent approved list. The original 70 entries and all source/history/rollback assertions remain unchanged. A broader scan reviewed migration/Flyway/history references, literal filename lists, count/size/range assertions, current versions and upgrade targets; the managed and protected-base migration assertions already have the correct current and historical boundaries.

The checkpoint's Security workflow jobs completed successfully, but its independent aggregate CodeQL failed on new High alert #221 in the UTC storage test. A constant-list time-zone value was concatenated into SQL. The test now uses a fixed `pg_catalog.set_config('TimeZone', ?, true)` prepared statement with a bound value. It additionally verifies the returned setting while preserving the actual application-role transaction, UTC/Moscow offsets, epoch and stored-Instant assertions, schema checks and rollback. No alert was dismissed and the failed aggregate is not reclassified.

These two test corrections change no production rule, migration byte, threshold, accepted Contract, Frozen Finding Set or production enablement. The next checkpoint requires a new complete local verification and exact current CI; these failed and focused records do not provide that admission. `production_write_enabled=false` and independent Controller review remain unchanged.
