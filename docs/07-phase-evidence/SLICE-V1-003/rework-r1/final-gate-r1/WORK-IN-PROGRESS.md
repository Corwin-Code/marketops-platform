# Final Gate residual rework — diagnostic progress

This is a working record, not engineering closure or Controller approval. The
Controller's W10 `NOT_PASS_EXISTING_FINDINGS_NOT_FULLY_CLOSED` is preserved in
`controller-package/`. The supplemental filename has the same bytes as that
report. The accepted Contract, Frozen Finding Set and Owner authorization hashes
remain unchanged.

The starting local and remote Head is
`3ff042df66d5d6924b587cac96fc652b93bf5e7a`, tree
`907aa20d6d38e116db9cddd9e54ded87a99cbe64`. PR #30 remains open Draft against
`08ad7da7d9e75b4ddd1c387a22ac0affba9e1430`, without auto-merge. Local diagnostic
runs below used a changing uncommitted worktree. They are not attributed to a
future publication commit or a GitHub run.

## Implementation and transitive findings under verification

- CV-A: freeze each consumed evidence-kind/purpose Profile, including exact
  scope/version digest; apply source and acceptance bounds independently;
  preserve applicable historical cohorts and cost proofs; reject stale segments,
  revoked/mutated profiles, incomplete reports and Provider incidents.
- CV-B: one cause-dependency rule across Java and SQL; exact Policy-authorized
  financial loss additionally retains fresh sellability/availability and all
  existing sales, isolation, exception and human-approval guards. Unknown profit
  remains unknown; unsupported causes cannot produce a clear Preview.
- CV-C: freeze the original action cause; read the exact advertising scope and
  source histories for the full observation window; keep risk-clearance and
  stopped exposure distinct from company/profit success. Source-only changes,
  expiry and revocation schedule one revision; unchanged sweeps schedule none.
  Invalidated Protection terminals reopen quarantine and the same advertising
  Task independently of the profit-axis verdict.
- CV-D: preserve the original capacity test. Add a separate synthetic mixed
  1,000-object workload with historical mature/revised Outcomes, expiry,
  invalidation and control states, targeted updates and hourly repair. Thresholds
  and the production worker cadence are unchanged. Historical rows are declared
  fixture inputs; this is not advertising APPLY throughput evidence.
- The mixed workload exposed `CRITICAL_SALES_GUARD` being emitted by the Case
  calculator but rejected by its database evidence-role constraint. V0068 adds
  that existing evidence type while retaining every historical row and role.
- Per-cohort Metric applicability is also being verified: a newer unrelated
  period cannot supply favorable costs to an old Outcome cohort merely because
  its evaluation timestamp is fresh.

V0066–V0070 are forward migrations in this worktree. V0001–V0065 are unchanged.
No production or shared environment was used; the PostgreSQL servers are owned
disposable TestDatabase containers. Provider calls use only the fixture port.
`production_write_enabled=false` and all 24 release obligations remain blocking.

## Bounded diagnostic evidence

The CV-B diagnostic command ran 87 unit tests and 25 real Spring/PostgreSQL
integration tests successfully in `/tmp/slice3-cv-b-focused-r3.log`, SHA-256
`515e6aa15ee39fc5d4053cad78283be76e146cba31ca7f2e84cc535e4e110449`.
Additional tests and source changes were made afterwards and still require the
full affected regression. This count does not certify the current worktree.

The CV-D diagnostic run reached the actual targeted workers after preserving 40
historical Retained observations. Its targeted assertions demonstrated 20
improvements and 20 regressions, preserved critical-guard Case evidence and the
shared-variant reservation rule. The final mixed receipt and full regression
remain pending; no capacity result is promoted by this working note.

The additive CV-E reconciliation independently validates the five historical
W8/W10 measurements. Local W8 measurements retain null GitHub identities; W10
backend-build and backend-integration retain their own run/job/artifact tuples.
Current closure matrices will be regenerated only from completed current-source
verification and exact publication/CI evidence.
