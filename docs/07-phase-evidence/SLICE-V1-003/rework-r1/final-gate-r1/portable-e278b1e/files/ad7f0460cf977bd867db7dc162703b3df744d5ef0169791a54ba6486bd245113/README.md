This additive private archive preserves two exact local backend source checkpoints. No original bytes were edited, no product tests were reexecuted, and nothing was published or copied into the repository.

- `7e66cf8-INDEX.json`: the full local backend checkpoint at 7e66cf87afc15773d4f6e6e6717a86e7b8336ae5 completed with 1,588 unit and 1,056 integration nodes, all passed. The source inventory and tree were stable and clean. This precedes the accepted section 6.24 OutcomePolicy resolution repair and is not final closure evidence for the later source.
- `98344bb-INDEX.json`: terminated partial checkpoint at 98344bb42025b300ae1265faccd232d9ed5437fc, exit 143, 1,588 unit and 379 integration nodes passed, three integration failures. It is failure history, not a completed backend verification.

Each deterministic ZIP contains every captured backend file (JAR, SBOM, raw JUnit, JaCoCo, log, source and identity inventories, layer receipt, and any completed capacity/resource datasets). The 983 archive additionally includes the original failure disposition. ZIP entries are lexicographically sorted with fixed 1980 timestamps and Unix file modes. Every member's CRC and SHA/size were reread and checked against the external INDEX. Original paths and all relative archive mappings are listed individually. All original byte hashes were checked again after creation.

`*-RAW-JUNIT-RECONCILIATION.json` counts actual raw testcase elements independently of the collector, preserving names, class, ordinal, status and exact report SHA. `*-SCOPE-RECEIPT.json` links the raw receipts, checks packaged `META-INF/build-info.properties` against exact Head, preserves separate root/class coverage values, and binds completed capacity datasets/resources without inventing a GitHub tested-merge/job/artifact identity. Root LINE counters match sourcefile line totals; class LINE sums differ because classes can share source-file lines. No measurement is changed.

Each publication scan inspected all original files and actual nested ZIP/JAR/GZIP/TAR member bytes, including class files. It used the seven governance patterns plus a JWT shape, with decoded XML/HTML and JSON string views. It did not restrict itself to summary JSON. Both scans completed, with originals unchanged and ZIP member CRC checks. Each had five raw pattern hits, all individually classified by member SHA/occurrence: four immutable dependency PEM delimiter/format-error constants and one regex match spanning distinct Tomcat class constant-pool entries. Actual class constant pools were parsed to establish these narrow exceptions; no full PEM payload, JWT or test token was found. The allowlist is exact per occurrence and does not exclude entire files, JARs or rules.

Raw hit rows remain unchanged in `HITS.jsonl`; the additive `HIT-CLASSIFICATION.json` records the exact scope and rationale. No token value is printed. These bounded patterns are not a universal PII or credential proof and do not themselves grant publication authority. Any test-token/JWT or unknown hit would remain private. Existing Owner and production-write boundaries remain in force.

The helper logs include two small failed reconciliation attempts (a missing optional evidence byte field and the observed packaged metadata path) followed by successful exact-byte reconciliation. These were archive-helper diagnostics, not product test executions or altered measurements.
