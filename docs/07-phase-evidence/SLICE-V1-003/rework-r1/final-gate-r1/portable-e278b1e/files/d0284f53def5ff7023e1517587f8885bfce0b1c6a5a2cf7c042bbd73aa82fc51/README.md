# Exact checkpoint Security evidence

This directory binds source Head `7e66cf87afc15773d4f6e6e6717a86e7b8336ae5`, tested merge `f9a1d73f324323d13ea72db3890090c9b9d85450`, exact shared tree `4fe4ffd46ff408ff97cd59a2b676c575a73982ac`, PR #30, and actual Security run **34001582060, attempt 1**. `SECURITY-RECEIPT.json` records **PASS_FOR_THIS_EXACT_SOURCE_CHECKPOINT** for this Security layer only.

All three Security jobs succeeded, and the separate aggregate CodeQL check **101401407869 succeeded**. The preceding checkpoint's HIGH #221 is now **fixed**, at `2026-09-06T00:37:13Z`, with null dismissal time, reason and comment. Its old most_recent_instance correctly remains the last-seen occurrence at the preceding failed merge; no current occurrence is invented. The new scoped fixed-alert response, absence from new Java SARIF, exact source patch and prepared SQL establish code repair. `ALERT-221-CODE-FIXED.json` preserves that reconciliation. No alert was altered or dismissed by this collector, and the preceding checkpoint's failure remains separate.

The source now uses a fixed `SELECT pg_catalog.set_config('TimeZone', ?, true)` prepared statement and binds each constant zone. It checks the setting result while retaining the original app-role transaction, UTC/Moscow offsets, stored instant and epoch assertions, timestamp type scan, rollback and final reads. Security's compiled-code analysis establishes the fixed source it analysed; the separate test execution remains a backend verification responsibility.

Java analysis **1730566375** contains 99 raw results; TypeScript analysis **1730561300** contains one. All 100 raw SARIF rule/location tuples match exactly 95 open quality alert instances plus five historical dismissed HIGH instances. Relative to checkpoint 98344bb, no open alert is added and only #221 is removed. All remaining 95 flagged expressions and their containing source files are byte-identical to the prior bounded review. The four test-only deprecation notes #222–225 remain open. This is not a zero-quality-findings or zero-raw-HIGH claim.

The actual manual Java CodeQL build compiled 669 main and 304 test source files with `./mvnw -B -ntp -DskipTests clean package` and succeeded. Original checkout, manual tracing, build and SARIF upload logs are preserved. This Security build does not run JUnit. Security uploaded zero Actions artifacts; the SARIF files are direct Code Scanning API responses with exact analysis IDs, categories, merge provenance and matching job times.

The actual clean execution inventory has 1272 files and SHA-256 `2f0e83ee4c70a445e9e8ac4191ef0bd86629b3639f5d7b6e3bb472939bfc6986`: 1270 inputs directly match remote source blobs and two follow pinned reversible CRLF checkout rules. Source and tested-merge trees are identical; the signed merge has the exact base `08ad7da7d9e75b4ddd1c387a22ac0affba9e1430` and source Head as parents. The source commit itself is unsigned. The actual parent EXPECTED-SOURCE and collector before files are preserved; the earlier locally derived binding is retained as initial history. This identity check makes no assertion that the full backend run has passed.

Default branch main still has four open HIGH Dependabot advisories for vulnerable fast-uri versions. This exact PR uses first-patched **3.1.6**, and successful Dependency Review reports four added dependency records with zero reported vulnerabilities, while removing 3.1.5 and four advisory records. The default branch alert state, PR dependency delta, npm audit and broader supply-chain audits are separate facts and layers.

Every API response, stderr and command/exit/hash record is retained. Three transient EOF responses remain under their original filenames; only successful later responses support the receipt. All 30 original log ZIP members pass CRC checks. The bounded raw API/log scan took **1.878 seconds** and inspected 119 streams. Its only raw/decoded matches are SHA-256-identical historical static CodeQL rule-help examples in the successful TypeScript SARIF. Unreviewed occurrences are zero; original bytes and matched secret-shape text are never rewritten or exported by the triage.

Machine evidence includes `SECURITY-RECEIPT.json`, `SOURCE-IDENTITY-COMPARISON.json`, `ALERT-221-CODE-FIXED.json`, `ALERT-DELTA-TRIAGE.json`, `HISTORICAL-DISMISSED-HIGH-RECONCILIATION.json`, `DEPENDABOT-DEFAULT-BRANCH-BOUNDARY.json`, `RAW-LOG-MEMBER-INDEX.json`, `RAW-PUBLICATION-SCAN.json` and `SECRET-SHAPE-TRIAGE.json`. Raw files remain in this directory. Additional archive compression is deferred until the parent full run releases its resource slot. No full backend/browser pass, independent Controller approval, Ready, merge or production enablement is claimed.
