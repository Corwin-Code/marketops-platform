# Verification corrections after source checkpoint 1381ef7

The exact function-privilege test still listed the 82 pre-V0072 callable functions, so the full local run on `1381ef78594875a1e1304e4d9a636e49d0fab71f`, tree `d366331b9b0ad7f8eb0f90c8536f46c0d2b9ce5e`, failed its strict equality check. The observed difference was exactly the four V0072 Outcome Policy resolution helpers. Each is a required application reader, SQL STABLE and SECURITY INVOKER, with PUBLIC EXECUTE revoked. No new write route or definer privilege was found. The test now appends only those four signatures, retains all original 82 and the exact equality assertion, and explicitly checks app EXECUTE, PUBLIC refusal and INVOKER state for the legacy resolver and all four helpers. Original private-function, write/delete, ownership and SQLSTATE refusal assertions remain unchanged.

Root stopped only the identified Maven process and its test descendants after this completed failure. The collector retained 2040 passing nodes and one failure, zero errors/skips, exit 143 and stable clean-source inventory `0270adc39585c868a5f6993b8b35324a2bd3b5addf3ebc6c67485269d43a061f`. The 315.103-second run is failed and interrupted, not full verification. It emitted no original or mixed capacity report; no capacity result is assigned to it. The focused correction then passed all 46 nodes across the complete privilege and policy-resolution classes plus the non-goals unit checks, on explicitly dirty source inventory `a9e7d39ea62aa4ff4b2cdf38857a25808c214f441495fc37b5976511585bf410`.

The checkpoint's Frontend workflow 34007484556 failed only lint job 101417260364; typecheck, build and test jobs succeeded, and formatting was skipped after lint failed. Three ESLint violations are corrected without relaxing behavior: the asynchronous test assertion uses a block body, its exact JSON-payload assertion first requires a string request body, and the API adapter uses nullish fallback after the existing malformed/null-array rejection. The original raw run logs, failed command and source/tested-merge metadata remain separate. The tested merge was `ddec24ca37d71a61772b6682e633d7536da1cb3e`, with the same tree as source 1381ef7.

A complete local frontend-quality diagnostic passed lock installation, lint, formatting, types, all 337 tests in 22 files, positive and negative coverage gates, bundle isolation and the final build. Its stable dirty-source inventory is `9706e844337851329c5e30954598162ac52144c82facaf736b04475c25f0c6ec`; elapsed time was 39.342 seconds. The framework's 77 nested suites are not 77 source files. An earlier diagnostic stopped at npm startup because npm 11 refuses one empty file used as both user and global config. It executed no product tests. The execution wrapper now uses two distinct empty config files; original environment isolation and quality commands are unchanged.

These narrow verification corrections leave all production controls, Policy values, migration bytes, accepted Contract and Frozen Finding Set unchanged. The next checkpoint requires a fresh complete nine-layer verification and exact CI evidence. Historical checkpoint reviews and failed/raw reports remain source-qualified and cannot supply that admission. PR 30 remains Draft, `production_write_enabled=false`, and independent Controller review remains pending.
