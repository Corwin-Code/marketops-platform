# Source rework after the failed 5fd53c0 checkpoint

This source checkpoint continues the same authorized R1 and the original CV-A–CV-E residual scope. Engineering completion and exact final CI are pending. The accepted Contract, Frozen Finding Set, Owner Decisions and existing 70 migration files remain unchanged; V0071 is append-only. Production write remains disabled.

The full local 5fd53c0 run failed with 2,612 actual test nodes (2,604 passed, four failures and four errors). Three migration assertions still expected 65 migrations; the sealed-authority fixture duplicated SELLABILITY when its old broad substitution reached the new ten-kind profile generator; the mixed workload measured P95 300,957 ms against the unchanged 300,000 ms limit. Original reports and the failed JAR remain failure evidence. The corresponding two CI jobs failed their early historical mixed-fixture setup before capacity was measured; those jobs have no mixed P95. Their old non-mixed capacity passes are preserved separately.

The migration assertions now require 71 migrations. The sealed fixture changes only its intended cause dependency and verifies that all ten canonical profile kinds remain distinct. The mixed fixture aligns only its historical preparation clock to PostgreSQL microsecond storage. A real JDBC/PostgreSQL negative test demonstrates that an unaligned synthetic timestamp rounds 211 ns into the future and correctly leaves evidence unavailable and the reservation active; the aligned positive test proves the actual canonical early-safety release. This reproducer establishes a precision defect without claiming that the unrecorded historical CI timestamp remainder has been recovered. Production freshness/future guards and advancing measurement clocks are unchanged.

A measured pre-optimization diagnostic exposed 48 application-level Freshness queries per object (six purposes by eight evidence kinds). The gatherer now resolves the same complete typed authority set with one bound batch query and no cache or default. Single and bulk resolution share the rule. The same-class scan found missing platform and optional-store restrictions and ambiguous same-rank semantic profiles. Actual application-role tests cover every scope, half-open validity, retired/absent profiles, full authority fields/digests, unrelated store/platform profiles and conflicting semantic scopes. V0071 changes only the two matching company-profile applicability predicates in the frozen-baseline validator; the existing canonical planner already enforced that rule. Independent aggregate admission remains intact, including the retained-sales-share refusal for an uncovered new store.

New Outcome records persist an explicit operational-association inference limit. Both intervention readers and the browser expose the persisted marker; absent or unsupported historical markers stay UNKNOWN. No historical rows are rewritten and no causal incrementality claim is inferred. Actual PostgreSQL tests also prove identical stored instants under UTC and Europe/Moscow sessions. A new application-role reservation regression proves that a released prior reservation cannot authorize generic same-object reentry: a fresh seal and reservation still receive the existing MO092 refusal, with the second command absent and authority/history unchanged.

The final diagnostic mixed run retained JaCoCo, 1,000 native objects, 250-item batches, 30-second gaps, historical Outcome/control states and all original thresholds. Its 1,040 critical samples measured P95 224,239 ms, maximum 260,904 ms, targeted wall 240,349 ms and full sweep 114,887 ms. Forty dropped late corrections were recovered, all 40 latest retained outcomes became REGRESSED, 10 exceptions expired and 10 invalidated, and the 41 actual write-gate checks remained refusals. Its source identity is the dirty worktree inventory SHA-256 `229d4a3f00dbf602f1064af005f82eea1063eb9ec7c404d51ea0e34a98ec95ea` on 5fd53c0, not a clean or final commit. This diagnostic pass does not replace the upcoming unfiltered backend verification or final containing-commit CI.

The final-gate assessment tools preserve separate per-criterion, per-Finding and per-clause reviews. Their structured adapters check exact raw JSON values, source identity and artifact hashes, and emit only pending candidates. Root must review and bind each actual positive/adverse proof after all nine layers finish successfully. AC200 remains a candidate-prerequisite assessment with independent Controller approval pending.
