# Existing Finding and acceptance matrix synchronization

This is an integration plan, not a closure verdict. The Controller report is
preserved as historical evidence at reviewed W10 Head. The original Contract,
Frozen Finding Set and all historical measurements remain unchanged.

The 17 Findings whose evidence the Controller accepted for the reviewed scope
retain that historical disposition. New source changes still need affected
regression; earlier acceptance does not automatically prove the new Head.
S3-DR-004, S3-DR-011, S3-DR-015, S3-DR-020 and S3-DR-022 require the following
explicit residual assessments before their engineering closure is regenerated.

| Check | Existing Findings | Required new evidence |
| --- | --- | --- |
| CV-A | S3-DR-004, S3-DR-015 | Source/use-specific frozen/applicable Freshness through the real Outcome application and isolated PostgreSQL; old source/new ingestion, incomplete windows, withdrawn versions, Provider incident and lawful mature windows |
| CV-B | S3-DR-004, S3-DR-011 | Exact Policy accepts the economic cause only with complete fresh one-sided harm proof; unchanged guards; common Java/SQL/Planner/Preview/Outcome cause dependencies; real positive and negative application/database paths |
| CV-C | S3-DR-015 | Frozen original reason and window independently distinguish cleared risk, stopped exposure and continuing harm for availability/sellability, including unresolved profit; preserve distinct inventory/Listing responsibility and efficiency result |
| CV-D | S3-DR-020 | Preserve the five historical bounded measurements; append declared-capacity representative mature/revised Outcomes and expired/invalidated control states through targeted updates and hourly repair with actual stage counts and unchanged latency gates |
| CV-E | S3-DR-022 | Reconcile each run independently, regenerate only from executed current-source evidence, and disclose the new exact Head/tree/tested merge with 12 required jobs plus aggregate CodeQL |

The direct acceptance references identified by the Controller are S3-AC-036,
043, 044, 045, 163 (CV-A); 091, 092, 099, 100 (CV-B); 160, 161, 162 (CV-C);
070, 197 (CV-D); and 199, 200 (CV-E). The accepted clauses and the same-class
scan determine any transitive acceptance references; these direct references
must not be treated as the maximum affected scope.

Preserve the existing 22 Finding rows and 200 exact criterion texts. Add the
Controller historical disposition, residual checks, new executed evidence
references and source identities to the engineering assessments. Do not infer
closure from a test count, filename, or a generated mapping. A missing positive,
negative, source-authority, mixed-load or regression proof remains incomplete.

The active canonical state must acknowledge the W10 `NOT_PASS` and the resumed
Codex work. After all residual implementations and evidence pass, it can record
the new candidate engineering result with independent Controller re-verification
pending. S3-AC-200 must continue to distinguish executed prerequisites from the
Controller's independent no-unresolved-Blocker/Major conclusion. Only the
Controller can replace its historical `NOT_PASSED` conclusion for a new Head.

Every new measurement must record sourceHead, tree where available, testedMerge,
run id/attempt, job id/name, artifact id/name/digest, dataset identity/digest,
exact command, runtime/resource bounds, timestamps, actual test/stage counts and
the intended proof scope. Local runs use explicit null GitHub identity fields
with a local run identity; never assign a later publication SHA to an earlier
measurement. Use JaCoCo report-root counters for the report-level gate and label
class/CSV sums separately. Do not sum repeated integration job nodes into unique
test totals.

The final publication remains append-only on the named branch and existing
Draft PR. All 24 release obligations remain production-blocking and
`production_write_enabled=false`. No Ready, merge, force-push, Provider or
shared/production access is part of this work.
