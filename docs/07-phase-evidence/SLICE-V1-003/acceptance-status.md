# SLICE-V1-003 — acceptance status

Two hundred criteria, each read against the repository and then challenged.
Every claim of VERIFIED was put to a second reader who opened the cited
evidence; eight did not survive that and were lowered. The counts below are
after those corrections, not before.

| Status | Count | What it means |
| --- | --- | --- |
| VERIFIED | 55 | a named passing test or an artefact somebody opened demonstrates it |
| PARTIAL | 132 | some of it is demonstrated and a named part is not |
| NOT_YET | 13 | nothing in the repository demonstrates it |

Two later passes re-read only the criteria the session's work touches. The
first, at `91fcfaa`, moved `S3-AC-071` from NOT_YET to PARTIAL. The second, at
`77faa37`, re-read the criteria closed or advanced by the end-to-end advertising
path, the task journal, the published daily brief and weekly review, the
property suites and the now-passing declared-capacity gate.

Nine moved to VERIFIED: `S3-AC-065`, `S3-AC-067`, `S3-AC-164`, `S3-AC-165`,
`S3-AC-182`, `S3-AC-183`, `S3-AC-184`, `S3-AC-185` and `S3-AC-195`. One moved
from NOT_YET to PARTIAL: `S3-AC-197`, because the declared-capacity gate now
passes and what is left is advertising's own load. Nine gained evidence
without gaining a status — `S3-AC-064`, `S3-AC-071`, `S3-AC-094`, `S3-AC-096`,
`S3-AC-138`, `S3-AC-151`, `S3-AC-163`, `S3-AC-187` and `S3-AC-194` — because in
each case a named part is still missing and the row says which. Every other
status is where the audit put it.

## No engineering closure claim is made

`CLOSED_ENGINEERING_WITH_DEFERRED_RELEASE_OBLIGATIONS` is the only claim this
Slice may make, and it requires all two hundred criteria executably verified.
They are not: 132 are partial and 13 have nothing behind them yet. The
deferred obligations are recorded separately in `deferred-release-register.json`;
what follows is the engineering work that remains, not an external dependency.

## What is not yet demonstrated at all

- **S3-AC-047** — missing/conflicted Freshness Profile blocks only affected purposes and cannot erase an independent Fresh danger.
  - Nothing resolves or evaluates a Freshness Profile at runtime (AdvertisingPolicyRepository.resolveFreshness has no callers), so purpose-scoped blocking cannot be observed; the nearest analogue, AdLaneResolverTest#TC-ADV-LANE-002, proves only that a data-defect blocker cannot silence a proven loss, and a test seeding profiles for some purposes only and asserting that a Fresh danger still raises Protection would demonstrate this criterion.
- **S3-AC-068** — outside coverage, staffed-clock handling and `OUT_OF_COVERAGE_ACTIVE_HARM` exposure remain simultaneously visible.
  - staffed_coverage_enabled/timezone/start/end and out_of_coverage_visible_from_minutes are stored in core.ad_human_slo_profile and read into AdvertisingPolicyRepository.HumanSlo but are never consulted by any caller, and the literal OUT_OF_COVERAGE_ACTIVE_HARM appears only in a V0038 comment and in the contract and design documents, never in code, schema or a view.
- **S3-AC-070** — Protection/Regression targeted update meets P95 <= 5 minutes and hard <= 15 minutes; other targeted state update meets hard <= 15 minutes.
  - AdvertisingSlo declares CRITICAL_DISTRIBUTION_TARGET (5 minutes), HARD_BOUND (15 minutes) and TARGET_PERCENTILE, and AdvertisingTargetedWorker.recordLatency writes ops.ad_slo_observation with a breached flag computed from the hard bound, but AdvertisingSlo.missedCriticalTarget has no caller, no percentile is computed anywhere, and no test exercises the latency recording — a measurement over recorded observations plus a test would be needed.
- **S3-AC-072** — Accepted Exception preserves the calculated lane and exact known consequence.
  - No advertising Accepted Exception exists: there is no ad_*_exception table in V0036-V0053, no service and no endpoint, and ops.availability_accepted_exception (V0030:1275) is bound by foreign key to availability cases; only the ADVERTISING_EXCEPTION_REQUEST action scope code in V0036:54 has been created.
- **S3-AC-073** — Exception requires exact cause/scope, evidence, consequence, Operations endorsement, Owner approval, period, review and Bundle.
  - Nothing in the repository records an advertising Exception's cause, scope, evidence, consequence, Operations endorsement, Owner approval, period, review or Bundle — an advertising accepted-exception table with those columns and their check constraints, plus a test, would be needed.
- **S3-AC-074** — valid Exception pauses only matching ordinary Action SLO, suppresses duplicate work visibly, cannot coexist with action intent, and invalidation reopens the same Case without automatic renewal.
  - With no advertising Exception entity there is nothing to pause an Action SLO, suppress duplicate work, refuse coexistence with an action intent, or reopen a Case on invalidation; the availability equivalent in V0030 is not reachable from an advertising case.
- **S3-AC-149** — reenablement never restores old decision assets.
  - Nothing invalidates prior decision assets on reenablement: AdvertisingContainmentRepository.reenable() only sets state, endorser, approver, reenabled_scope and reenabled_at, mart.ad_case supersession (V0044) is driven solely by the cause no longer calculating, and a PENDING ops.ad_bid_command that survived the stop would pass the gate again once containment clears — a test that reenables a containment and shows the pre-stop command, candidate and recommendation refused rather than resumed would demonstrate it.
- **S3-AC-154** — material confounder keeps early safety Guard active but blocks action-specific final verdict.
  - The only occurrence of 'confounder' in the backend is the recalculation trigger class 'CRITICAL_SALES_OR_CONFOUNDER' in V0038; there is no material-confounder state, no column on core.ad_outcome_policy or ops.ad_outcome_observation, and nothing that keeps the early Completed-Sales Guard active while blocking an action-specific final verdict — a confounder state plus a test showing the Guard still evaluating while the verdict is withheld would demonstrate it.
- **S3-AC-156** — operational association and causal incrementality remain separate.
  - No causal or incrementality construct exists to be kept separate — grep finds 'incrementality' only in the criteria documents, there is no holdout, uplift or counterfactual basis (V0049 ad_outcome_policy_basis_ck admits only two pre-change bases), and nothing labels an OPERATIONAL verdict as associative; a marker on the observation distinguishing association from a causal claim, plus a test, would demonstrate it.
- **S3-AC-160** — `VERIFIED_AD_RISK_CLEARED` proves only the original advertising danger cleared.
  - `VERIFIED_AD_RISK_CLEARED` appears nowhere in the repository; the advertising outcome vocabularies are OutcomeEvaluation.Verdict (IMPROVED/UNCHANGED/REGRESSED/INDETERMINATE/NOT_YET_EVALUABLE) and DualAxisVerdict.Outcome, neither scoped to the original danger — the nearest mechanism is the cause-specific plan (V0050 ad_outcome_policy.cause_code), and a cause-scoped terminal state with a test would be needed.
- **S3-AC-161** — `VERIFIED_AD_EXPOSURE_STOPPED` proves only no new dangerous exposure under complete evidence.
  - `VERIFIED_AD_EXPOSURE_STOPPED` appears nowhere in the repository, and no state asserts 'no new dangerous exposure under complete evidence' — a terminal state that requires CANONICAL_CONFIRMED evidence over the exposure window, with a test, would demonstrate it.
- **S3-AC-171** — Gate EV is exact Platform+Store+Capability+object+direction+basis+value+window+exposure+actor+abort authority and cannot authorize another scope.
  - No Gate EV authority object exists: the only carrier is ops.ad_decision_policy_bundle.gate_scope_reference (V0041), a free text column bounded only by length 1..256, and value, window, actor and abort authority are not part of any authorization record — deferred-release-register.json records S3-REL-016 and gate_ev NOT_AUTHORIZED, so a structured, scope-checked Gate EV envelope plus a test that it cannot authorize an adjacent scope is what would demonstrate it.
- **S3-AC-172** — Gate E consumes operating foundation, Shadow, direction-specific Gate EV, failure controls, early safety, ownership and aggregate Exposure.
  - There is no Gate E artefact, evidence bundle or consumption check anywhere in the repository — deferred-release-register.json records gate_e NOT_AUTHORIZED and defers the layered Pilot Evidence Bundle as S3-REL-018; a Gate E record naming the operating foundation, Shadow, direction-specific Gate EV, failure controls, ownership and aggregate exposure, with a test, would demonstrate it.

## What is partially demonstrated

Each row names the part that is missing rather than only that something is.

| Criterion | Missing |
| --- | --- |
| `S3-AC-006` | Ingestion, Metric, Policy, workflow, Command, Readback and audit authority are each covered by a passing architecture rule; the Bundle is the one named authority with no such rule. |
| `S3-AC-012` | The relationship schema and the separation from control granularity exist, but 'remain queryable' has no reader: no repository, service, API or test queries core.ad_object_relationship. |
| `S3-AC-013` | The per-platform profile mechanism is fully modelled and constrained, but no Ozon or Wildberries profile content exists and nothing demonstrates the two platforms' semantics staying different. |
| `S3-AC-015` | Lineage and affected-set invalidation are demonstrated for the case identity and the entity digest, but bidding-mode continuity is captured and never checked, and no test exercises invalidation of a frozen executable asset. |
| `S3-AC-016` | Scope narrowing and DB privilege separation are implemented and the privilege half is tested, but no test drives an escalation attempt through the advertising read API or the command-creation function. |
| `S3-AC-017` | Cross-store narrowing and complete-affected-set identification are implemented in code, but finance detail is disclosed to every ADVERTISING_VIEW holder and neither half has a test. |
| `S3-AC-018` | The two role-disjoint action scopes and the separation-of-persons rules on containment and bundle activation exist, but the per-command endorsement step and any evidence-scope precondition for endorsing or approving are absent. |
| `S3-AC-019` | Variant masking is visible against the complete count, but no state distinguishes a masked value from an absent one and neither half of the criterion has a test. |
| `S3-AC-020` | Only the API surface exists and enforces the contract; the export, notification, attachment and AI advertising views the criterion names have not been built, so nothing demonstrates the contract being applied uniformly. |
| `S3-AC-021` | Raw provenance, source times and append-only correction history are enforced for both configuration and spend/traffic facts, but only configuration observations record the Semantic Profile identity they were read under. |
| `S3-AC-023` | Corrections are recorded without overwriting history and superseded rows are excluded on read, but no code path turns a late or corrected Spend fact into a recalculation request. |
| `S3-AC-024` | Provider-attributed numbers are stored separately with their window and model and are never merged into canonical values, but no view or evidence row surfaces them as Provider observations. |
| `S3-AC-026` | The estimated label and the write refusal are both enforced and tested, but nothing ties an ESTIMATED_ALLOCATION row to refusal of a Settled result or a high-risk Recommendation (OutcomeEvaluation's guard never inspects evidence state), and no console renders the label because frontend/marketops-console contains no advertising code at all. |
| `S3-AC-027` | Order, Completed and Retained are distinct enum members whose mixing is refused, but there is no Settled sale stage anywhere — 'Settled' exists only as an outcome stage and as ad_allowable_cpa_definition.contribution_basis — so the fourth stage the Contract §6.5 names is unmodelled. |
| `S3-AC-028` | The early guard exists and is unit-tested, but it is driven by elapsed hours since the matching readback plus a settled-coverage ratio rather than by fresh attributable Completed Sales, and nothing implements the 30-day Retained Sale observation as the final sales-protection stage (SalesPreservation has no caller in main and no retained-window column exists on core.ad_outcome_policy). |
| `S3-AC-029` | The company-total term of the conjunction is implemented and unit-tested, but nothing binds it to the complete affected set or to an action-time frozen rule — SalesPreservation has zero callers in src/main and no table stores a frozen preservation plan. |
| `S3-AC-030` | The non-compensating conjunction is proven, but the 'action-time frozen' required-unit set is not: the units are supplied by a caller that does not exist, and mart.ad_case_variant_diagnostic.is_critical_sales_unit is recomputed per calculation rather than frozen against a command. |
| `S3-AC-031` | Missing critical-unit evidence yields UNRESOLVED with a named reason ahead of any pass-or-fail arithmetic, proven by passing unit tests; the rule has no production caller yet, so it is proven as a rule rather than as an observed pipeline outcome. [audit: SalesPreservationTest#TC-ADV-SALES-004/005 exist and pass (6/6, surefire report confirmed), and SalesPreservation.java does return UNRESOLVED with COMPANY_TOTAL_EVIDENCE_UNRESOLVED / CRITICAL_SALES_UNIT_EVIDENCE_UNRESOLVED ahead of any pass-or-fail arithmetic. But `grep -rln '\bSalesPreservation\b' backend/marketops-server/src/main/java` returns exactly one file — SalesPreservation.java itself. No service, repository, projection writer or c… |
| `S3-AC-032` | The formula and the component-completeness rule are unit-tested, but there is no distinct fulfillment input (it is folded into platformFeesPerUnit) and AdvertisingCaseCalculationService.profitOf hard-codes promotionCostPerUnit as NOT_AVAILABLE, so the wired path always blocks — AdvertisingEfficiencyFlowIT#TC-AD-FLOW-003 asserts official spend is present and neither profit axis is. |
| `S3-AC-033` | Both axes are computed and reported separately and the case row carries a policy version digest, but AdvertisingContributionProfit.compute takes no version argument and nothing pins a computed profit to the mart.metric_definition versions (ADVERTISING_CONTRIBUTION_PROFIT v1, CONTRIBUTION_PROFIT_PER_AD_RUB v1). |
| `S3-AC-034` | The non-compensating Pareto rule plus the sales gate is proven by passing unit tests; DualAxisVerdict has no caller in src/main, so no running pipeline has yet produced this verdict. [audit: The nine DualAxisVerdictTest cases exist and pass (surefire: Tests run 9, Failures 0), and DualAxisVerdict.evaluate does check sales preservation, then unresolved axes, then material worsening, then material improvement. But DualAxisVerdict is dead code: `grep -rln '\bDualAxisVerdict\b' src/main/java` returns only DualAxisVerdict.java, and `grep -rn 'VERIFIED_EFFICIENCY_SUCCESS' src` finds it only in that file and its own test — no migration, no mart column, no AdvertisingCaseView field, no frontend ref… |
| `S3-AC-035` | A still-negative absolute contribution downgrades to IMPROVED_NOT_HEALTHY and does not close the responsibility, proven by a passing unit test; as with S3-AC-034 the type is not yet wired into any service. [audit: DualAxisVerdictTest#TC-ADV-AXIS-004 exists, passes, and asserts IMPROVED_NOT_HEALTHY; DualAxisVerdict.java:118 returns it with ABSOLUTE_CONTRIBUTION_PROFIT_STILL_NEGATIVE and closesResponsibility() (line 137) is true only for VERIFIED_EFFICIENCY_SUCCESS. The rule is correct. The gap is the same as S3-AC-034: IMPROVED_NOT_HEALTHY appears nowhere outside DualAxisVerdict.java and DualAxisVerdictTest.java — not in any of V0036-V0053, not in AdvertisingCaseView, not in frontend/. There… |
| `S3-AC-036` | Stage-distinctness, window, frozen policy version and gap reason codes are modelled and unit-tested, but the observation row carries no Freshness and no Confidence column, no service or test has ever written an ad_outcome_observation, and mart.ad_case's contribution profit carries no operational/settled basis. |
| `S3-AC-037` | The observation-only half is proven by passing tests; the 'company total over clicks' half rests on the schema vocabulary having no such linkage basis and on AdvertisingEvidenceRepository's numerator query being scoped to the exact ad_native_object, stage and window, not on an explicit refusal test. [audit: The observation-only half holds: AdLinkedConversionTest#TC-ADV-CONV-002/003/004 and MaxCpcTest#TC-ADV-CPC-003 pass (AdLinkedConversionTest XML: tests=22, failures=0), and V0037:153-155 ad_conversion_definition_linkage_stage_ck does force PROVIDER_REPORTED_LINKAGE to equal sale_stage PROVIDER_NATIVE_OBSERVATION. The second half — 'company total sales divided by ad Clicks is rejected as ca… |
| `S3-AC-039` | The gap-blocks-Max-CPC half is unit-tested end to end, but the Provider-to-canonical discrepancy exposure is code and schema only — no test seeds provider attribution alongside linked sales, so AD_ATTRIBUTION_GAP_RATIO has never actually been computed. |
| `S3-AC-040` | Stage mixing is rejected by passing tests, but duplicate return-loss counting is prevented only by CHECK constraints that no test ever attempts to violate, so the criterion's 'tests reject ... duplicate loss counting' clause is unmet. |
| `S3-AC-041` | Recomputation and the ceiling-not-target bound are proven by unit tests and the governing versions are recorded as mart.ad_case_evidence rows, but the 'visibly' half is undemonstrated because there is no advertising surface in frontend/marketops-console. |
| `S3-AC-043` | The schema carries evidence kind, purpose and platform/store/semantic scope, but there is no Account scope at all, the only resolver's WHERE clause handles ORGANIZATION/PLATFORM/STORE and silently cannot select a SEMANTIC_PROFILE-scoped row, and resolveFreshness has zero callers in src/main and zero tests, so no freshness is resolved at runtime. |
| `S3-AC-044` | Every named dimension exists as a column and is read into a record, but nothing turns them into a freshness verdict and mart.ad_case_evidence's FRESHNESS_PROFILE role is never written by AdvertisingProjectionWriter, so no dimension has ever affected a decision. |
| `S3-AC-045` | The schema can express both asymmetries, but no code evaluates a profile against a fact, so neither 'a newly ingested old fact is not automatically Fresh' nor 'a mature cohort is not automatically stale' has been observed; a test seeding core.ad_freshness_profile and asserting a verdict would demonstrate it. |
| `S3-AC-046` | The rule is expressed as a SQL function plus an activation trigger and as row-level floors, but no test ever activates a bundle — AdvertisingGraphFixture deliberately seeds the bundle as DRAFT so the trigger short-circuits — so ad_freshness_purpose_violations has never executed, and no test references DecisionPurpose. |
| `S3-AC-048` | The monotonic ordering is expressed as data and checked by a function the bundle activation trigger calls, but no test invokes core.ad_qualification_tier_is_monotonic or activates a bundle (every fixture bundle is DRAFT), so a non-monotonic tier set has never actually been refused. |
| `S3-AC-049` | Every named dimension except the sustained signal is enforced — AdvertisingPolicyRepository.QualificationPolicy.minimumSustainedPeriods is read and never compared against mart.ad_case.sustained_cycles, and the policy's own minimumSourceCoverageRatio/minimumAffectedSetCoverageRatio are unused — and optimizationQualified itself has no test because no integration test reaches the Optimization lane. |
| `S3-AC-050` | Immature and immaterial signals do fall to Watch with a named non-actionable cause, but sustainment never reaches the decision — AdLaneResolver.Signals has no sustained input and minimum_sustained_periods is never applied — so an unsustained-but-material signal is indistinguishable, and 'no automatic Task' currently holds only because the advertising module contains no task-raising path at all. |
| `S3-AC-052` | The missing-Profile half is demonstrated, but the conflicted half is not: V0037's ad_optimization_qualification_policy_no_overlap only makes same-scope overlap impossible, no QUALIFICATION_POLICY_CONFLICTED state exists, and no test isolates a missing qualification Profile from the other two policies or asserts that no Bid Increase proposal is produced. |
| `S3-AC-054` | The one-sided reversibility gate is demonstrated, but the Fresh half is not: AdvertisingPolicyRepository.resolveFreshness has no caller anywhere in main, so no freshness Profile gates a danger proof before Protection is raised. |
| `S3-AC-056` | Lane-first ordering and the reason-coded hard sub-tiers are verified, but the within-lane order is not lexicographic: AdPriorityPolicy.rank sums seven weighted terms into one compensating commercial value, which Contract §6.10 permits only as an analysis display and not as the canonical order. |
| `S3-AC-057` | Regression/unresolved-execution reaching P0 is tested and the Unknown/Mismatch and Quarantine inputs are wired, but compensation responsibility is hard-coded absent at AdvertisingCaseCalculationService.java:93 (the third Signals argument is the literal false) and no test exercises the quarantine, unknown/mismatch or compensation branches. |
| `S3-AC-058` | Sellability→P1 and economic harm→P2 are tested, but the critical-sales limb is dead in production wiring — AdvertisingCaseCalculationService.java:96 always passes OneSidedDangerProof.none() for criticalSalesDanger and SalesPreservation.evaluate has no caller in main — and neither the availability→P1 nor the critical-sales→P1 branch has a test. |
| `S3-AC-059` | The non-compensating Data Repair over Optimization separation and Watch's lack of action authority hold, but neither lane has its own factor order: core.ad_priority_policy (V0038:41-89) carries a single weight set and AdPriorityPolicy.rank applies the same seven terms to every lane. |
| `S3-AC-060` | Rank and due time have no AI or user-personal input by construction, but escalation is not implemented anywhere for advertising and no named test asserts this property. |
| `S3-AC-061` | Cause routing is verified on the Case view and a Protection/Optimization bid proposal does create one task, but no Task is created for a material Data Repair case (every Data Repair case carries a blocker, so AdvertisingProposalService returns empty) nor for a Protection cause with no bid direction, ops.task carries no role so the Task itself is not cause-routed, and no test covers advertising task creation. |
| `S3-AC-062` | Repeated calculation and the case-identity uniqueness are demonstrated, but nothing exercises concurrency (there is no two-writer test) and nothing exercises the active-Task dedupe in RecommendationService.proposeBidChange's liveFor guard. |
| `S3-AC-063` | Separate owner-split Cases are implemented and the value object admits them, but they are Cases rather than Tasks (Data Repair raises no Task), mart.ad_case has no linked-case column so the relation is only implicit through ad_native_object_id, and no test drives the calculation service to the two-case outcome. |
| `S3-AC-064` | Both stages are now recorded as well as declared. ops.work_task_event (V0055) carries VIEWED, ACKNOWLEDGED and ACTION_RECORDED as separate kinds and the schema refuses one that carries another's payload, so an acknowledgement and a first attributable action are distinguishable in the record rather than only in the profile. What remains is a consumer: only action_minutes is read, at AdvertisingProposalService.java:216, and nothing yet computes an acknowledgement deadline from core.ad_human_slo_profile.acknowledgement_minutes. |
| `S3-AC-066` | The cause-owner routing for material Data Repair is verified, but nothing compares lanes: there is no constraint, code path or test ensuring a PROTECTION profile's minutes are no weaker than an OPTIMIZATION profile's, and no Regression profile exists at all. |
| `S3-AC-069` | A missing Profile does block the Bundle-dependent write path, but it does not leave the human side unresolved: AdvertisingProposalService.java:215-218 silently substitutes a 15-minute review window when resolveHumanSlo returns empty, which is exactly claiming a service level nobody published, and no test covers the absent-Profile case. |
| `S3-AC-071` | The cadence the audit named as unproven is now measured for the declared-capacity portfolio: one sweep over 5,000 variants completes in 169,843 ms against a 3,600,000 ms cadence, leaving 3,430,157 ms of margin, and it repairs all 50 deliberately dropped targeted triggers while every lane's SLO observations are preserved. What remains is that the portfolio measured is the availability one; no advertising sweep has been measured at a declared advertising capacity. |
| `S3-AC-075` | The Ozon side of the manual shadow workflow is demonstrated end to end, but Wildberries is absent: the only WILDBERRIES row anywhere in the schema is the platform row in V0004, there is no Wildberries platform.ad_semantic_profile or ad-bid-change capability, and no test exercises it. |
| `S3-AC-076` | Object, affected set, current/target configuration, evidence, expiry and verification plan are bound and exercised, but bundle_id and guardrail_evaluation_id are nullable columns with no foreign key and the only issue path never populates a Bundle or an approval route. |
| `S3-AC-078` | Identity and scope are structurally required, but nothing refuses a packet whose permission, guardrail evaluation or required approval is unresolved: the approving columns are nullable, blocker_codes is unconstrained, and no service mediates issuance. |
| `S3-AC-079` | The readback grade is admitted and may prove a configuration, but no test records an unconflicted API readback proving one, and the three proving grades are treated as equal - nothing ranks readback strongest or supersedes a weaker grade with it. |
| `S3-AC-080` | The export grade exists and can prove a configuration, but the verification row carries no source-profile reference and no replayability evidence beyond a free-text evidence_reference, and no test records an export at all. |
| `S3-AC-081` | Distinctness of the verifier and the recorded field/value/observed-at/evidence are demonstrated; the verifier's authorisation is not - the only requirement is a user_account row in the same organization, with no role or action-scope check. |
| `S3-AC-082` | The self-report half is enforced in schema and demonstrated, but an incomplete observation is not modelled at all - there is no completeness field or grade for a partial screenshot, so one logged as INDEPENDENT_MANUAL_VERIFICATION would still prove the configuration. |
| `S3-AC-083` | No manual row can carry provider idempotency, application time or a command success, but the evidence grade itself is an unchecked caller parameter, so nothing stops a manual observation being labelled OFFICIAL_API_READBACK with no attempt behind it. |
| `S3-AC-084` | The reservation and exposure axes are kind-agnostic by construction, but no code path ever takes a CONFIRMED_MANUAL_PACKET reservation and no test exercises one, so a manual intervention consumes nothing today. |
| `S3-AC-087` | Semantics, grid, capability, credential and readback are each covered by a named passing test, but the current-Bid and permission checks exist only inside ops.create_ad_bid_command, which no test ever calls - AdvertisingGraphFixture seeds commands directly and says so. |
| `S3-AC-088` | Structural unreachability of the unverified provider path is demonstrated several ways; the Shadow-only state is not - nothing in production code issues or exposes a manual packet, so an unverified path currently surfaces no shadow state to anyone. |
| `S3-AC-090` | Write-grade conversion, Max CPC and controllability are demonstrated, but the qualification gate itself is untested, the freshness profile is defined and never consulted, and sellability/availability only bar an increase indirectly, via the lane ladder, and only when every promoted variant is unsellable or unavailable. |
| `S3-AC-093` | The headroom mechanism keeps a target below the ceiling and the schema makes headroom mandatory for MAX_CPC_BOUNDED policies, but the domain still selects the ceiling exactly when headroom is absent and no test covers the constraint that keeps such a policy out of the database. |
| `S3-AC-094` | Determinism is now generated rather than only exemplified: 500 randomly drawn fact and policy combinations reproduce the same candidate, and the end-to-end path records the same bounded value the domain computes. The remaining gap is unchanged — the policy declares a candidate count but AdvertisingProposalService.java:165 only ever records ordinal 1, so there is a deterministic candidate rather than a deterministic finite set. |
| `S3-AC-095` | Free-typing is structurally refused because the target must equal the named candidate's stored value, but that agreement check inside ops.create_ad_bid_command is never executed by a test and there is no operator surface - backend or frontend - through which a candidate can be selected or rejected. |
| `S3-AC-096` | The first half is now proved end to end as well as by construction: the normalized amount is recorded before Preview, carried unchanged through approval and command creation, transmitted, and read back equal to the approved target with the match state derived by the database from the frozen pointers. A generated property adds that every candidate that exists is one the grid would accept unchanged. What is still untested is the negative: no case shows a rounded or substituted target producing a non-matching readback. |
| `S3-AC-098` | A bounded Protection target can stop above the ceiling and that is tested, but the Contract's RECOVERY_IN_PROGRESS_NOT_HEALTHY status does not exist anywhere and no policy field explicitly permits such a target. |
| `S3-AC-099` | The exact-cause requirement is enforced in schema and in allowsCauseBoundStep and the reversibility property is proven in OneSidedDangerProof, but nothing re-checks the proof at candidate generation and no test exercises the cause-bound route through AdvertisingProposalService or the policy's cause list. |
| `S3-AC-100` | The Preview does report an unavailable Max CPC through maxCpcState and the MAX_CPC_UNAVAILABLE guardrail reason, but nothing states that a cause-bound target limits exposure only and proves nothing about profitability, and no test covers a cause-bound preview. |
| `S3-AC-101` | Estimated and provider-observed inputs are proven unable to produce a candidate by named passing tests; "similar-SKU inference" and "AI magnitude" are covered only by absence (AdLinkedConversion admits no inferred numerator and a grep of advertisingefficiency finds no AI/LLM input at all), not by a dedicated refusal test. [audit: The estimated-conversion/Max-CPC and provider-observation halves are demonstrated by passing tests (AdLinkedConversionTest#TC-ADV-CONV-002/003/004/013, MaxCpcTest#TC-ADV-CPC-003/004, BidCandidateTest#TC-AD-BID-009/010), but the criterion also names similar-SKU inference and AI magnitude and nothing executable touches those: AdvertisingNonGoalsTest's three marker li… |
| `S3-AC-102` | The multi-axis, non-compensating thresholds and the four fixed hard triggers exist and are constrained in schema, but only ordinary_nonzero_envelope_amount is ever read — material_spend_exposure_amount, material_affected_variant_count, material_critical_sales_amount, material_relative_change_ratio and material_cumulative_change_amount are consumed by no SQL or Java in the repository. |
| `S3-AC-103` | Only the role split is demonstrated: no code path requires or records an AD_BID_CHANGE_ENDORSE step before approval (the enum constant is referenced nowhere but ActionScopeCode), and no migration seeds a materiality policy, so "the initial ordinary nonzero envelope is zero" is asserted only in comments in V0045 and AdvertisingDecisionRepository. |
| `S3-AC-104` | Unresolved materiality fails closed and self-endorsement is blocked for manual packets, containment re-enable and bundle activation, but the controlled AD_BID_CHANGE approval path itself records no maker and enforces no maker/endorser/approver distinctness. |
| `S3-AC-105` | Before/After, affected set, versions, bundle, route and blockers are in the preview shape, but critical sales, Spend, profit and inventory/sellability are absent from AdBidImpactPreview/AdvertisingBidProjection, and no test or console surface exercises POST /recommendations/{id}/ad-bid-impact-preview. |
| `S3-AC-107` | Recheck at lease and at the transmission boundary is demonstrated by passing tests, but the "earliest bound" computation covers only two bounds (lease policy and approval scope) and has no test of its own — evidence, policy-version and credential bounds are not folded into the expiry. |
| `S3-AC-108` | Non-renewability is a schema check, and every expiry/containment refusal is re-derived from live rows at each gate call, so nothing in the repository can extend, rebind or resurrect a lapsed approval. [audit: Expired (AdvertisingTransmissionBoundaryIT#TC-AD-BOUNDARY-004) and killed (#TC-AD-BOUNDARY-003) are observed, but quarantined, superseded and materially-invalidated are not — a grep of the whole test tree for QUARANTINE_ACTIVE, RECOMMENDATION_STALE and AUTHORIZATION_INVALID_OR_EXPIRED finds them only in PriceWritePathIT and ErrorCodeTest, never for the advertising gate; and the cited AdvertisingPrivilegeBoundaryIT#TC-AD-PRIV-102 does not cover ops.approval_decision at all, since READ_O… |
| `S3-AC-109` | The Ordinary route is unconditionally refused, but no promotion record, table or evidence precondition exists anywhere, so nothing demonstrates the all-Material mature-evidence requirement or the per-command Maker-Checker rule a promotion would have to satisfy; no test drives an ORDINARY_IMPACT command through the gate. |
| `S3-AC-110` | The four fixed triggers are unfalsifiable in schema and standing-policy authorization is refused for AD_BID_CHANGE by an unconditional guard clause, although that specific refusal has no test of its own. [audit: The fixed-trigger half is a real schema check (ad_materiality_policy_fixed_triggers_ck, V0039:255), but the Standing-Policy half is source only: the guard at ApprovalService.java:155-160 is never executed by any test (grep of src/test for POLICY_AUTHORIZATION_UNUSABLE/authorizeByPolicy returns only ErrorCodeTest's vocabulary list), the cited standing_policy_automation marker scan cannot reach it because AdvertisingNonGoalsTest.sources() walks only advertisingefficiency/ and db/migra… |
| `S3-AC-114` | Status/readback-before-anything-else is demonstrated, but the capability profile's idempotency_semantics column is written and never read, so nothing in the code makes native idempotency conditional on a verified, evidence-backed profile. |
| `S3-AC-117` | The safe side holds — no same-command mutating retry is possible at all — but the criterion's positive preconditions are unimplemented: there is no representation of verified NOT_APPLIED evidence and no path that would consume it together with an unchanged prior bid and current authority. |
| `S3-AC-120` | Exact amount and currency equality with no tolerance is enforced in the only function that can set a match state, but the bid unit is copied from the command rather than compared against the observed representation, so a CURRENCY_MAJOR/CURRENCY_MINOR mismatch would still read as MATCHES_TARGET. |
| `S3-AC-122` | Concurrent same-object entry is blocked and a prior action must discharge its observation duties before it releases, but there is no reentry profile, cooldown or calibration record anywhere in schema or code, so nothing prevents a further ordinary action once the earlier reservation is released. |
| `S3-AC-123` | The captured prior bid is immutable and the restore stays on the original command row and reservation, but no test ever executes an advertising compensation, so the target-equals-captured-prior-bid rule is demonstrated only by the trigger body and the privilege boundary. |
| `S3-AC-124` | Prior matched readback, current-state ownership and the current hard gates are enforced in SQL, but the human half of the criterion is absent — there is no route that records a new Preview, an Operations endorsement, an Owner approval, an action-bound Stop Condition/Regression or a Gate scope covering compensation. |
| `S3-AC-127` | The controlled path provably reserves the complete resolved affected set, but no code path or test ever takes a CONFIRMED_MANUAL_PACKET reservation (the kind exists only in the V0041 CHECK), and ops.take_ad_action_reservation does not itself verify that the variant array it is handed equals the named affected set's. |
| `S3-AC-128` | Configuration resolution, early observation and unknown/mismatch are all exercised, but the 'no unresolved Regression' leg is only a CHECK constraint and a branch in the release function — nothing in main or test ever sets regression_open true. |
| `S3-AC-129` | A non-overlapping action is correctly still subject to the envelope in code, but no test admits a non-overlapping action and then blocks it on an exhausted axis, and only three of the six declared axes are computed at all. |
| `S3-AC-130` | The lane ordering is deterministic but only decides which blocker is named: a Protection action arriving second against a live OPTIMIZATION reservation is refused identically by ops.take_ad_action_reservation, so there is no precedence, preemption or queue-jump for overlapping scope. |
| `S3-AC-131` | Reuse of the original reservation by the compensation is readable in the gate but untested; nothing anywhere detects a material cross-domain change, blocks isolation, or confounds/rebaselines an Outcome — a repository-wide grep for confound/rebaseline/cross-domain finds only that one trigger enum and a comment. |
| `S3-AC-132` | Four of the six axes bind (count, headroom, unresolved writes, cumulative change); max_affected_retained_sales_share and max_associated_spend_amount are never read by any gate, query or worker — a repository grep finds them only in V0041 and the test fixture INSERT (AdvertisingGraphFixture.java:299). |
| `S3-AC-133` | There is genuinely no cross-axis or cross-direction arithmetic, but 'all aggregate axes' is only three of six today and no test seeds an envelope and exhausts a single axis to observe the others still passing. |
| `S3-AC-134` | Unknown and Mismatch demonstrably keep consuming capacity by state, but governed manual interventions consume nothing in practice because no code path ever creates a CONFIRMED_MANUAL_PACKET reservation, and no test populates either axis with rows. |
| `S3-AC-136` | Aggregate capacity is revalidated inside the transmission function itself, and the approval lease is fixed at command creation and re-checked on every evaluation, so waiting expires it rather than extending it. [audit: The first half and the "nor extends while waiting" half hold: ops.open_ad_bid_command_attempt (V0043:173-179) does re-run ops.evaluate_ad_bid_write_gate — envelope axes included (V0053:262-307) — for APPLY and RESTORE, PlatformCallSpecRepository.adBidAttemptCurrent re-runs the same gate again immediately before the socket, and approval_expires_at is INSERTed once by ops.create_ad_bid_command (V0045:214-225) with no UPDATE anywhere in any migration and no UPDATE privilege for … |
| `S3-AC-137` | The missing-envelope refusal is proven on the mirrored query path only — the gate's own branch is never reached because AdvertisingGraphFixture always seeds an ACTIVE envelope — and there is no conflicted-envelope detection: the gate silently takes one row by scope precedence and accepts status RETIRED as well as ACTIVE. |
| `S3-AC-138` | The path is now observed end to end rather than only constructed: a late fact restates a settled window, the revised evaluation regresses past the plan threshold under a satisfied guard, ops.reopen_ad_lineage_after_regression opens an ACTION_OUTCOME_QUARANTINE at affected-set scope naming the observation that caused it, and the write gate then refuses the next command with QUARANTINE_ACTIVE and nothing else. The gaps the audit named remain: the scope is digest equality rather than overlap, there is no object-scoped quarantine, and nothing invalidates unexecuted decision assets. |
| `S3-AC-139` | The provider-integrity half is exact and demonstrated, but an AUTHORITY_VERSION_QUARANTINE quarantines nobody: ops.ad_active_containment cannot match it (its object id and digest are NULL) and a repository grep shows authority_version_reference is written but never read. |
| `S3-AC-140` | Scoping is structurally correct, but no test contains one object or store and then shows a second, non-overlapping object or store still passing the gate — the isolation is inferred from the predicate rather than observed. |
| `S3-AC-143` | Revocation is provably confined to the unexecuted packet and the uncertain/verification states behave correctly, but nothing connects a quarantine or kill to revoking outstanding packets, and no test attempts to revoke a reported or verified packet to observe the refusal. |
| `S3-AC-144` | The four stop kinds and their scopes are representable and enforced by ops.ad_active_containment, but nothing enforces a maximum authorized scope per actor: no constraint ties containment_kind or activator to a permitted scope_kind, there is no role check on activation, and no API surface exposes it. |
| `S3-AC-145` | Reason, evidence, actor, scope and time are all mandatory and activation provably needs no Owner, but a review owner is not recorded for an operator- or trigger-activated emergency stop: accountable_role_code is nullable and is set only by the automatic regression reopen. |
| `S3-AC-147` | Every named condition is a table-level requirement and one outstanding condition is observed to block a lift, but the roles are not checked at all — any two distinct users satisfy 'Operations endorsement' and 'Owner approval' — and no test drives a missing ordinary condition (root cause, unknowns, authority evidence) to observe its refusal. |
| `S3-AC-150` | No automatic reverse write can be produced from a regression or a cause, but the first half is contradicted by the code: a quarantine or kill closes the gate for every direction, so an Exact Compensation is not separately eligible during quarantine — ops.lease_ad_bid_compensation would refuse it. |
| `S3-AC-151` | The API write path is now shown to freeze a complete plan before external execution rather than only prevented from doing otherwise: the command names the bundle, the bundle names the outcome policy, and both outcome stages are afterwards evaluated against that frozen policy version. The gap is unchanged: ops.ad_manual_execution_packet has a nullable bundle_id and a free-form verification_plan, so a manually executed action still freezes no Outcome Evaluation Plan. |
| `S3-AC-152` | Windows, versions, baseline basis, coverage, sample, action and affected set are bound, but the Case is not — it is only joined at evaluation time through candidate → mart.ad_case in AdvertisingOutcomeRepository.due() — and neither critical sales units nor any confounder policy appear on the plan or the observation at all. |
| `S3-AC-153` | Baseline derivation is deterministic in code and the basis is fixed by a pre-existing policy row, but no test exercises AdvertisingOutcomeService, AdvertisingOutcomeRepository or core.resolve_ad_outcome_policy at all, and nothing asserts that an AI path cannot supply or alter the baseline. |
| `S3-AC-155` | No benchmark is invented — an unusable baseline produces an inconclusive verdict with a named reason — but the reason code `OUTCOME_BASELINE_INSUFFICIENT` does not exist anywhere in the repository, and there is no distinct 'insufficient comparable history' test (history depth or window comparability) separate from an absent or zero baseline value. |
| `S3-AC-157` | The resolver is exact and default-free by construction, but no test executes it, no production code calls it (AdvertisingOutcomeRepository.due() joins the bundle's outcome_policy_id directly), and ops.ad_bundle_validation_failures does not check the outcome policy is in force at the bundle's effective_from the way it checks the conversion, CPA and target policies. |
| `S3-AC-158` | A missing decision authority demonstrably blocks the write while the observation and the proven-harm case survive, but nothing ties a missing or conflicting *Outcome* Policy specifically to blocking a final success or a dependent Optimization write — ops.ad_bundle_validation_failures never inspects the outcome policy and no test covers an outcome-policy conflict. |
| `S3-AC-159` | The frozen plan is what the recalculation reads by construction, but the SETTLED_REVISED late-adjustment path has no test of any kind and nothing asserts that a newer policy version cannot be substituted at recalculation time. |
| `S3-AC-162` | Per-cause case identity means resolving one cause cannot close another, and a partial improvement provably does not close responsibility, but there is no advertising Protection terminal state at all and no inheritance link from an advertising case to an inventory or listing cause, so 'does not close the inherited cause' cannot yet be observed. |
| `S3-AC-163` | A formal operational verdict is now produced by the real worker against the real schema, and it is visibly not settled: the guard state is NOT_APPLICABLE and the settled coverage ratio is null, which is different from a guard that passed. The audit's other two gaps stand — nothing pins the operational window to thirty days, and the advertising console still carries no outcome fields. |
| `S3-AC-166` | History is provably preserved, but nothing upgrades an earlier Operational no-improvement — the supersession mechanism only applies to a prior settled observation (ad_outcome_observation_revision_shape_ck requires stage SETTLED_REVISED), no reader combines the two stages, and no test covers the upgrade. |
| `S3-AC-167` | Short settlement coverage provably cannot become a final settled result and the estimated-allocation table is not read by the outcome path, but there is no explicit 'settlement attribution unresolved' state in the outcome vocabulary and no test asserts that an estimated allocation is refused as outcome evidence. |
| `S3-AC-168` | The regression routes an accountable role and quarantines the lineage by construction, but no test exercises the reopen, and 'removes settled-success reuse' is not modelled at all — no settled success is recorded as reusable justification anywhere, so nothing can remove it. |
| `S3-AC-169` | No real provider side effect is reachable and many fail-closed states are proved by named passing tests, but completeness is not established — the register itself records closure_claim_made=false ('S3-AC-001..200 are not all executably verified'), and executable-evidence.md lists coverage, mutation, browser end-to-end, SLO and whole-repository regression as not run. |
| `S3-AC-170` | Read and diagnosis are demonstrated for one platform (OZON, synthetic fixture) plus a read-only console, but Wildberries has no advertising rows, profile or test anywhere, and no Task, Manual Shadow or Outcome business surface is exposed to a user — the Outcome path has no test at all. |
| `S3-AC-173` | Everything untested is demonstrably disabled and the scope-exceeding refusal codes exist, but there is no Gate E scope object to compare against demonstrated evidence, and the BUNDLE_SCOPE_EXCEEDED / DIRECTION_NOT_ENABLED / CANDIDATE_BASIS_NOT_ENABLED branches themselves are exercised by no test because no fixture ever activates a bundle. |
| `S3-AC-174` | The register records the 30-day and Settled outcomes as continuing, production-blocking obligations rather than prerequisites, but nothing in the repository expresses the Gate E Amendment requirement for future expansion — the register has no amendment field and the gates are simply marked NOT_AUTHORIZED. |
| `S3-AC-177` | Stage consistency, Provider semantics and both monotonicity checks are implemented, but materiality_policy_id, approval_lease_policy_id and exposure_envelope_id are NOT NULL foreign keys the validation function never inspects, Gate scope is only 'gate_scope_reference IS NOT NULL' in ad_decision_policy_bundle_activation_ck rather than a comparison against Release/Gate evidence, and no test drives the function past BUNDLE_NOT_FOUND. |
| `S3-AC-178` | Three distinct approvers and a gate-scope reference are structurally required, but nothing checks that the endorser holds an Operations role or the approver is the Owner (both are plain iam.user_account FKs), the bundle's security_attestation_present column (V0041:474) is read by no constraint, trigger or query anywhere in the repository, and no test activates a bundle. |
| `S3-AC-179` | There is no bundle activation routine at all — activation is a plain UPDATE guarded by a validation trigger — so nothing invalidates old unexecuted decision assets for the scope, and no test observes activation, its atomicity, or lineage preservation across a supersession. |
| `S3-AC-180` | The blocking half is demonstrated at transmission and at the approval guardrail, but no rollback routine exists (nothing writes a new bundle_version on rollback and no test covers it), and the candidate-side refusal in AdvertisingDecisionService.reasons line 163 ('BUNDLE_UNRESOLVED') has no test of any kind. |
| `S3-AC-181` | The queue and case view show lane, protection tier, cause, accountable role, evidence state, blockers, six measures and rank factors only; Contract 6.33 also requires Task/assignee/human SLO, Approval and expiry, Exception/Hold/Quarantine/Kill, active intervention and aggregate exposure, Command/manual action/Provider state/Readback and Protection/Operational/Settled Outcome, none of which appear in AdvertisingCaseView.tsx or in the backend AdvertisingCaseView record, and no integration test exercises /api/v1/console/advertising. |
| `S3-AC-186` | The AI boundary is proved for the existing diagnosis projection, but ProjectionBuilder names no advertising source (grep for 'advertis' in ProjectionBuilder.java returns nothing) and there is no AI drafting of any advertising artifact, so the 'uses authorized canonical references' half is undemonstrated for this Slice. |
| `S3-AC-187` | The brief surface adds a state distinction the audit's list did not yet have anywhere: an empty section that was covered and an empty section that had no source are visibly different, and a reading that supersedes an earlier one shows the earlier one rather than replacing it. The console re-derives completeness and restatement from the body's own fields rather than trusting the flags it is sent. The wider gaps in this criterion are unchanged. |
| `S3-AC-188` | No test of any kind exercises /api/v1/console/advertising (grep for 'console/advertising' in src/test returns nothing), so scope inheritance and pagination are asserted only by reading the controller; the backend's offset parameter is sent by no client and covered by no test. |
| `S3-AC-189` | Keyboard use is unverified — grep for keyDown, Tab, tabIndex or axe across frontend/marketops-console/src and tests/browser returns nothing — and Store-local display is absent: the advertising views never render asOf or any timestamp and no timezone conversion exists anywhere in the console. |
| `S3-AC-190` | SCAN_EXTENSIONS in validate_governance.py (line 1555) excludes .sql and .md, so none of the 18 new migrations or any document is secret-scanned; verify:bundle has no recorded run at this head; and nothing anywhere scans specifically for Buyer PII (fixtures/ contains only README.md, so there are no advertising fixtures to have scanned). |
| `S3-AC-194` | Two more duplication routes are now closed and observed. A brief publication is idempotent on content, so a second pass over an unchanged period writes nothing; and an outcome observation for a stage that already exists is not re-recorded, because the due query excludes commands whose stage is present. The audit's remaining gaps in this row are unchanged. |
| `S3-AC-196` | No mutation-testing tool is configured in this repository and none was added; these suites are mutation-sensitive by construction, which is what a mutation score is a proxy for, but no mutation score exists. |
| `S3-AC-197` | The declared-capacity gate the Controller named now passes, on the full dataset and with the threshold untouched. RepresentativePerformanceIT drives AvailabilityRecalculationScheduler.recalculateWhatChanged over 5,000 variants and 5,000 accepted facts: CRITICAL-lane p95 226,229 ms against a 300,000 ms budget, maxMillis 236,035, zero breaches in any of the five lanes, 50 dropped triggers all repaired by the sweep, and the hourly reconciliation completing in 169,843 ms with 3,430,157 ms of margin against its cadence. Nothing was weakened: the threshold is unchanged, the test is not excluded, and the dataset is the base's full 1,047,420 metric values. What is still absent is advertising's own … |
| `S3-AC-198` | There is no advertising analogue of AvailabilityConsoleAuthorizationIT#TC-CONSOLE-001..010 — nothing tests that an unauthenticated or ungranted caller is refused /api/v1/console/advertising or that another organization's case is absent — and role-minimal disclosure at the API is therefore unobserved (no notification subsystem and no advertising export path exist to test). |
| `S3-AC-199` | SLICE-V1-003 has no acceptance-status.md (both SLICE-V1-001 and SLICE-V1-002 have one), so no committed Acceptance-ID to design/code/test/evidence mapping exists; docs/04-api/V1_CAPABILITY_MATRIX.md:133 and :135 still assign AD_BUDGET_CHANGE and AD_CAMPAIGN_STATE_CHANGE to SLICE-V1-003 although AdvertisingNonGoalsTest#TC-ADV-NONGOAL-001/005 forbid them; CURRENT_STATE.md carries no slice_v1_003_as_built_design or slice_v1_003_executable_evidence pointer and still records FULL_SCOPE_IMPLEMENTATION_IN_PROGRESS; and seven docs/06-runbooks/advertising-*.md files were still untracked and being written while I checked. |
| `S3-AC-200` | validate_production_readiness.py still exits non-zero on TC-GLOBAL-002: three comments in V0040 and V0047 narrate what the schema used to do. The rule is right and the only way to satisfy it is to edit two migrations every clean install has applied and whose checksums MIGRATION-INVENTORY.json publishes, which forward-only discipline forbids and which no later migration can correct. The conflict between the two rules is an Owner decision. The other three checks now pass: the approved migration set carries V0036-V0053, and the tolerant-schema-creation rule no longer reads PL/pgSQL's IF NOT EXISTS (SELECT ...) as DDL. No CI or security-scan result is recorded at this head. |

## What is verified

| Criterion | Evidence |
| --- | --- |
| `S3-AC-001` | docs/03-work-items/SLICE-V1-003-advertising-traffic-efficiency.md: sha256 1606a844934c49a9e67dc0a1a15d49f4003913efc678bae94403c3c29ecb811c, 129400 bytes, git blob 669c38dc4d9429249e663da0e684dabf570c… |
| `S3-AC-002` | docs/00-governance/CURRENT_STATE.md: active_slice_contract, active_slice_contract_sha256, active_slice_contract_git_blob_sha1, active_slice_contract_bytes/lines, slice_v1_003_source_protected_main 08… |
| `S3-AC-003` | docs/00-governance/CURRENT_STATE.md: active_delivery_slice SLICE-V1-003, active_gate SLICE_V1_003_FULL_SCOPE_IMPLEMENTATION, authorization FULL_SCOPE_IMPLEMENTATION, active_slice_contract_authorizati… |
| `S3-AC-004` | docs/03-work-items/V1_DELIVERY_SLICES.md line 52: the SLICE-V1-003 row keeps the Advertising & Traffic Efficiency outcome and names `AD_BID_CHANGE` on Ozon and WB with status CONTRACT_ACCEPTED_FULL_S… |
| `S3-AC-005` | backend/marketops-server/src/test/java/com/mimococo/marketops/advertisingefficiency/AdvertisingNonGoalsTest.java#TC-ADV-NONGOAL-002 'no adjacent inventory capability is reopened by this Slice' — scan… |
| `S3-AC-007` | docs/08-handoffs/OWNER-SLICE-V1-003-CONTRACT-ACCEPTANCE-EVIDENCE.md: 'separate_preimplementation_design_gate: NOT_REQUIRED' alongside implementation_authority FULL_SCOPE_IMPLEMENTATION_WITHIN_EXECUTI… |
| `S3-AC-008` | docs/00-governance/CURRENT_STATE.md: gate_ev NOT_AUTHORIZED, gate_e NOT_AUTHORIZED, production_deployment NOT_AUTHORIZED, production_write_enabled false, merge_authorization NOT_AUTHORIZED_SEPARATE_L… |
| `S3-AC-009` | V0038__create_advertising_case_projection_and_orchestration.sql lines 183-250: mart.ad_case declares organization_id, store_id, platform_code, ad_native_object_id, affected_set_id, semantic_profile_i… |
| `S3-AC-010` | V0036__create_advertising_identity_and_official_facts.sql lines 395-436: core.ad_affected_set is append-only with affected_set_digest (ad_affected_set_digest_ck '^[0-9a-f]{64}$'), product_variant_ids… |
| `S3-AC-011` | backend/marketops-server/src/test/java/com/mimococo/marketops/database/AdBidWritePathIT.java#TC-AD-WRITE-105a 'a bid change against a listing variant is unrepresentable' reads pg_get_constraintdef fo… |
| `S3-AC-014` | BidCandidateTest#TC-AD-BID-004 'an unverified or incomplete grid describes no write' — every unusable ProviderBidGrid (UNVERIFIED, absent bid field, null step, null precision) yields usable()==false … |
| `S3-AC-022` | AdMeasureAndAffectedSetTest#TC-ADV-MEAS-002 'an unavailable measure is not zero', #TC-ADV-MEAS-004 and #TC-ADV-MEAS-005 (an AVAILABLE measure without a value, and an unavailable one carrying a value,… |
| `S3-AC-025` | AdLinkedConversionTest#TC-ADV-CONV-002/003/004 — a provider observation can never be write-grade, a provider stage cannot be smuggled through the write-grade factory, and a hand-built provider conver… |
| `S3-AC-038` | AdLinkedConversionTest#TC-ADV-CONV-001 a complete write-grade conversion is the linked events over eligible traffic |
| `S3-AC-042` | AdLinkedConversionTest#TC-ADV-CONV-008 too few linked events refuses a write-grade conversion |
| `S3-AC-051` | AdLaneResolverTest#TC-ADV-LANE-004 "a material data defect is Data Repair, never a quiet Watch" — a signal set that is otherwise qualified, material and controllable still resolves to DATA_REPAIR whe… |
| `S3-AC-053` | AdLaneResolverTest#TC-ADV-LANE-001 through #TC-ADV-LANE-010 — every rung of the ladder returns exactly one lane, and the Decision record refuses a Protection lane without a sub-tier or a non-Protecti… |
| `S3-AC-055` | AdLaneResolverTest#TC-ADV-LANE-008 "an unproven danger does not raise Protection" — unknown alone yields WATCH, not a decrease |
| `S3-AC-065` | V0055__record_what_happened_to_a_task_and_who_did_it.sql: work_task_event_view_shape_ck refuses a VIEWED row with an action kind, action evidence, an outcome kind or an evidence reference |
| `S3-AC-067` | V0055__record_what_happened_to_a_task_and_who_did_it.sql: ops.work_task.first_raised_at, work_task_first_raised_ck and the BEFORE INSERT OR UPDATE trigger work_task_first_raised_at_is_held raising MO… |
| `S3-AC-077` | AdvertisingManualShadowIT#TC-AD-MANUAL-001 asserts ops.ad_bid_command exists while ops.ad_budget_command and ops.ad_status_command do not, and that platform.platform_capability contains neither 'ad-b… |
| `S3-AC-085` | AdvertisingNonGoalsTest#TC-ADV-NONGOAL-006 parses V0040__widen_write_registry_for_ad_bid_capability.sql and asserts the write-shape validation names exactly the capability codes ad-bid-change and pri… |
| `S3-AC-086` | AdBidChangeParameterContractTest#TC-AD-PARAM-001 and AdBidParameterContractParityIT#TC-AD-PARAM-005 run AdBidParameterCases.all(), which refuses 'a budget carried alongside a bid' (extra targetBudget… |
| `S3-AC-089` | backend/marketops-server/src/main/resources/db/migration/V0039__create_advertising_target_materiality_and_manual_shadow.sql: ad_bid_target_policy_version_uq and ad_bid_target_policy_no_overlap key on… |
| `S3-AC-091` | AdLaneResolverTest#TC-ADV-LANE-008 an unproven danger does not raise Protection; the ladder in AdLaneResolver.resolve enters Protection only on OneSidedDangerProof.established() |
| `S3-AC-092` | backend/marketops-server/src/main/resources/db/migration/V0039__create_advertising_target_materiality_and_manual_shadow.sql: ops.ad_bid_candidate.target_policy_id and target_policy_version are NOT NU… |
| `S3-AC-097` | BidCandidateTest#TC-AD-BID-012 an increase never passes the ceiling (step would allow 60, ceiling 41, target 41.0000); #TC-AD-BID-013 the step limit binds when tighter; #TC-AD-BID-014 a bid already a… |
| `S3-AC-106` | V0042__create_ad_bid_command_outbox_readback_and_gate.sql:450-531 ops.ad_bid_authority_snapshot binds proposal, platform/native keys, lineage generation, semanticProfileId, affected set (id/digest/re… |
| `S3-AC-111` | V0042__create_ad_bid_command_outbox_readback_and_gate.sql:155-160 CONSTRAINT ad_bid_command_idempotency_uq UNIQUE (idempotency_key) plus the format check |
| `S3-AC-112` | V0043__create_ad_bid_attempt_lifecycle_and_readback.sql:104-220 ops.open_ad_bid_command_attempt requires the current fence and lease, re-evaluates ops.evaluate_ad_bid_write_gate for APPLY/RESTORE, an… |
| `S3-AC-113` | V0042__create_ad_bid_command_outbox_readback_and_gate.sql:1029-1068 ops.lease_ad_bid_command (the only RETRY_WAIT -> LEASED route) updates state, fence_token, attempt_no, lease_owner, lease_expires_a… |
| `S3-AC-115` | V0043__create_ad_bid_attempt_lifecycle_and_readback.sql:163-170: "A mutating operation happens at most once per command, ever" — open_ad_bid_command_attempt raises MO090 if any APPLY (or RESTORE) att… |
| `S3-AC-116` | AdvertisingTransmissionBoundaryIT#TC-AD-BOUNDARY-006 "an unknown result never becomes a second mutating call" (timeout/unknown path) |
| `S3-AC-118` | V0042:221-225 CREATE UNIQUE INDEX ad_bid_command_live_uq ON ops.ad_bid_command (ad_native_object_id) excluding only the five terminal states, so an unresolved command blocks a second one for the same… |
| `S3-AC-119` | AdBidWritePathIT#TC-AD-WRITE-102c "success is reachable only from a pending readback or a person" — READBACK_MATCHED has only READBACK_PENDING and MANUAL_RESOLUTION as predecessors |
| `S3-AC-121` | V0043:445-451 the match CASE uses only equality — no tolerance band and no nearest-value branch exists |
| `S3-AC-125` | AdBidWritePathIT#TC-AD-WRITE-102b "a third value leaves automation entirely" — LATER_CHANGE_OR_MISMATCH_INVESTIGATION -> MANUAL_RESOLUTION only |
| `S3-AC-126` | V0049__create_advertising_outcome_plan_and_lineage.sql: business Outcome lives in ops.ad_outcome_observation (immutable via ops.ad_outcome_observation_is_immutable), a table with no relationship to r… |
| `S3-AC-135` | V0053__the_write_gate_must_refuse_rather_than_raise.sql: IF command.direction <> 'EXACT_PRIOR_BID_COMPENSATION' AND active_count > envelope.max_active_interventions - envelope.reserved_recovery_headr… |
| `S3-AC-141` | AdvertisingTransmissionBoundaryIT#TC-AD-BOUNDARY-003 — with a live lease and held fence, the gate contains no containment reason before activation and contains KILL_SWITCH_ACTIVE after, and ops.open_… |
| `S3-AC-142` | AdBidWritePathIT#TC-AD-WRITE-102d — to_state TERMINATED_WITHOUT_PROVIDER_CALL has from_states exactly LEASED, PENDING, RETRY_WAIT (containsExactly), so no transmitted state can be terminated as if no… |
| `S3-AC-146` | AdvertisingReservationIT#TC-AD-CONTAIN-002 (one person cannot lift their own stop) — reenable(activator, activator) and reenable(other, other) both raise DataIntegrityViolationException; only two dis… |
| `S3-AC-148` | AdvertisingReservationIT#TC-AD-CONTAIN-003 (a security cause needs an attestation before anything restarts) — with all five ordinary conditions met, outstandingConditions() is exactly ['SECURITY_ATTE… |
| `S3-AC-164` | AdvertisingOutcomeService.java: ORDER_STAGE for OPERATIONAL and RETAINED_STAGE for SETTLED; baselineFrom = observedFrom.minus(length), so both windows are read identically |
| `S3-AC-165` | V0050__cause_specific_outcomes_and_same_lineage_reopen.sql: reopen_ad_lineage_after_regression refuses anything but a guarded settled regression, and returns the containment already open for the same… |
| `S3-AC-175` | AdvertisingTransmissionBoundaryIT#TC-AD-BOUNDARY-002 'leasing is refused before anything is prepared' — the refusal message names BUNDLE_UNRESOLVED, so no command transmits without a resolved bundle |
| `S3-AC-176` | AdvertisingEfficiencyFlowIT#TC-AD-FLOW-005 and #TC-AD-FLOW-006 publish the conversion definition, allowable-CPA definition and qualification policy versions |
| `S3-AC-182` | V0056__publish_the_daily_brief_and_weekly_review_as_projections.sql: core.ad_reporting_calendar and ops.ad_brief_publication with as_of, cursor_feed_code, cursor_position_at and cursor_position_item_… |
| `S3-AC-183` | AdvertisingBriefService.WEEKLY_SECTIONS — the twelve topic codes, and SECTIONS_WITHOUT_A_CANONICAL_SOURCE naming why two of them cannot be filled |
| `S3-AC-184` | V0056: mart.ad_brief_item with ad_brief_item_one_subject_ck requiring num_nonnulls(case_id, work_task_id, recommendation_id, outcome_observation_id, slo_observation_id, containment_id, reservation_id… |
| `S3-AC-185` | V0056: ops.ad_brief_publication with supersedes_publication_id, revision_no, revision_kind, adjustment_reason and late_fact_reference; ad_brief_publication_is_immutable() raising MO044 |
| `S3-AC-191` | AdvertisingNonGoalsTest#TC-ADV-NONGOAL-001: no budget_command, BUDGET_APPLY, pause_command, strategy_switch, bidding_mode_write, negative_keyword, creative_write or portfolio_reallocation marker appe… |
| `S3-AC-192` | docs/00-governance/CURRENT_STATE.md:220 `production_write_enabled: false`, alongside gate_ev: NOT_AUTHORIZED, gate_e: NOT_AUTHORIZED and pilot: NOT_AUTHORIZED |
| `S3-AC-193` | `git diff --stat main...HEAD -- backend/marketops-server/src/main/resources/db/migration` shows 18 files, 7736 insertions and 0 deletions: V0001-V0035 are byte-untouched on this branch |
| `S3-AC-195` | surefire at this head: Tests run: 1480, Failures: 0, Errors: 0, Skipped: 0 — including 71 architecture cases and 22 property cases |
